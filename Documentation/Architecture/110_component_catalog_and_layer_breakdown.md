**Purpose**: Enumerate every `archon_search` module, its role, and the layer it belongs to.
**Audience**: Engineers locating where a change belongs; reviewers checking layering.
**Status**: Draft
**Last reviewed**: 2026-06-24
**Next review**: 2026-08-20

# Component Catalog and Layer Breakdown

This is the module-level map of `archon_search/`. One row per module, grouped by layer. Use it to find where a change belongs before reading code. Cross-cutting flows are in [100_system_architecture_overview.md](100_system_architecture_overview.md); the wire surface is in [600_api_reference_or_public_interface.md](600_api_reference_or_public_interface.md).

## Principles

1. **One module, one layer.** A module that touches two layers (e.g. parser code in a route handler) is a smell — escalate before merging.
2. **Layers depend downward only.** Server depends on Query/Ingest; Query/Ingest never imports Server.
3. **Names mirror responsibilities.** `routes_*.py` are HTTP edges; `*_meta.py` are metadata models; `_types.py` is the dataclass spine.
4. **Underscored modules are internal.** `_types.py`, `_diagnostics.py`, `_helpers.py`, `_durable_io.py` are not part of the public import surface.
5. **No reaching across siblings.** Pipeline stages talk to each other only through `SearchPipeline`; route modules talk to each other only through `app.state`. (#Unverified — `routes_collections.py` currently imports `IngestRequest` and `_default_ingest_task` from `routes_jobs.py` at module level; this is a type/helper import rather than runtime state sharing, but it bends the rule.)

## Layer summary

| Layer | What it owns |
|---|---|
| Ingest | Parsing, chunking, ACL resolution from disk |
| Index | Embeddings, vector + FTS store, collection metadata |
| Query | Hybrid search, reranking, search-with-context, ACL filtering |
| Routing | Multi-collection selection via centroid pre-ranking |
| Metadata | Per-collection descriptions and centroids |
| Server | FastAPI app, MCP app, middleware, route modules, schemas |
| CLI | Click entry points and subcommands |
| Platform | OS service install/uninstall (launchd, systemd, Windows) |
| Telemetry | Opt-in JSONL writer/reader/pruner |
| Jobs | Async job lifecycle for long-running ingest/reindex |
| Eval | Deterministic backends + harness for the regression gate |
| Cross-cutting | Config, constants, types, progress, key manager |

## Ingest

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/parser.py` | Extract text from files, routing by extension: plain text and code via `Path.read_text`, HTML via trafilatura, PDF and images (OCR) via docling, Office formats (`.docx`, `.pptx`, `.xlsx`, `.xls`, `.rtf`, `.epub`, `.eml`, `.msg`) via markitdown. Raises `ParseError` on unrecoverable failure. | `DocumentParser`, `ParseError` |
| `archon_search/chunker.py` | Split parsed Markdown into chunk records of a target size. Assigns `start_offset`/`end_offset` (character offsets into the post-front-matter text) to each `ChunkRecord`; these transient fields are used by the enricher and are not persisted to LanceDB. | `DocumentChunker` |
| `archon_search/enricher.py` | **C3a** — Scan a text document for ATX/setext/RST headings (fenced-code-block exclusion applied) and populate `_heading` / `_section_path` in each `ChunkRecord.metadata`. `prepare(text)` builds an immutable `HeadingTable` (sorted by offset); `enrich_chunk(chunk, *, heading_table=None, page_table=None)` bisects the table to resolve the nearest preceding heading and its ancestor chain. Empty strings are returned for chunks preceding all headings and for non-text (binary) formats. **C3b** — `preprocess(text)` is a sibling method for docling-parsed sources (PDF/image): it excises `PAGE_BREAK_MARKER` strings, builds a post-removal `(offset, page)` coordinate table, and returns `(cleaned_text, page_table)`. The `enrich_chunk` page branch uses the table to populate `_page_start` (always present for docling sources) and `_page_end` (only when the chunk spans a page boundary). `is_docling_source(subtype)` and `source_subtype_for(suffix)` are module-level helpers that gate the pipeline's page-break path. `PAGE_BREAK_MARKER` and `PAGE_BREAK_MARKER_LEN` are module-level constants; the marker is an internal implementation detail and never reaches `ChunkRecord.text` or the FTS index. | `MarkdownEnricher`, `HeadingEntry`, `HeadingTable`, `PAGE_BREAK_MARKER`, `PAGE_BREAK_MARKER_LEN`, `source_subtype_for`, `is_docling_source` |
| `archon_search/code_enricher.py` | **C3c** — AST-based code symbol context enrichment for source-code files. `CodeEnricher` follows the same `prepare()` / `enrich_chunk()` two-pass protocol as `MarkdownEnricher`. `prepare(text, ext, file_path, collection_root)` invokes tree-sitter (optional `[code]` dep) to parse the source and build a `ScopeTable` of `ScopeEntry` records (each covering a function, method, or class scope); missing grammars and parse failures degrade gracefully. `enrich_chunk(chunk, scope_table)` resolves the innermost scope for each chunk's `start_offset` via bisect and returns a 5-field metadata dict. `CODE_EXTENSIONS` is the frozenset of dispatched extensions; `_module_path()` derives a dotted module path from the file path and optional collection root. Tree-sitter is never imported at module level, so the module is importable without `[code]` installed. | `CodeEnricher`, `ScopeEntry`, `ScopeTable`, `CODE_EXTENSIONS` |
| `archon_search/_path_safety.py` | Validate caller-supplied ingest paths at the request boundary (HTTP `POST /collections`, `POST /ingest`; MCP `ingest_file`, `ingest_directory`). Rejects empty/whitespace-only/NUL/non-absolute/`..`-traversal inputs. Symlink resolution and absolute-path scope are intentionally **not** validated (deferred to a future `allowed_dirs` feature). | `validate_ingest_path`, `PathUnsafeError` |
| `archon_search/acl.py` | Parse `_acl` front matter and `.acl` sidecars; resolve effective ACL per document; filter search results by request namespace. **E0b**: `read_acl_sidecar()` return type changes to `tuple[list[str] | None, list[str]]` — ACL entries and a warnings list. Sidecars > 64 KB return `(None, [warning_message])`. `resolve_acl()` return type changes identically. | `resolve_acl`, `apply_acl_filter`, `is_acl_allowed`, `read_acl_sidecar`, `parse_acl_value`, `is_acl_namespace_valid` |
| `archon_search/language_detector.py` | **C2** — Per-document language detection using the fasttext `lid.176.ftz` model. Lazy-loads the model; detects language on the first 2000 chars of Markdown output; strips `__label__` prefix; normalizes to ISO 639-1 (2-letter) or ISO 639-3 (3-letter) via `_FASTTEXT_ISO_MAP`; returns `"unknown"` when confidence is below `SearchConfig.language_detection_confidence_threshold` or text is empty. Runs in a thread pool to avoid blocking the event loop. Only active when `config.multilingual=True`. The models directory is resolved lazily on every call via `get_fasttext_models_dir()` (derived from `ARCHON_SEARCH_DATA_DIR`); see C9 Task 2.5. | `LanguageDetector`, `FASTTEXT_MODEL_FILENAME`, `get_fasttext_models_dir` |

## Index

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/embedder.py` | Dense-embedding façade with a Protocol backend. Default backend is fastembed. | `Embedder`, `EmbedderBackend`, `ModelEmbedder`, `make_embedder` |
| `archon_search/embedder_cache.py` | LRU cache for `Embedder` instances keyed by model name. Avoids reloading the same model weights when multiple collections use different models in the same process. Capacity is controlled by `[database].embedder_cache_size` (default 3); `[database].eager_load_embedders = true` pre-warms all known collection models at startup. Evicts the least-recently-used model when the cache is full. | `EmbedderCache` |
| `archon_search/store.py` | LanceDB-backed store: vector ANN + FTS, RRF fusion in `hybrid_search`, document/chunk lifecycle, namespace + ACL migrations, collection metadata persistence. `hybrid_search_with_trace` is a thin instance-method delegate to the module-level `_hybrid_search_with_trace`, which returns `list[ScoredSearchCandidate]` with full score provenance — used by `SearchPipeline.explain`. Predicates are built via `_where_eq`/`_where_in` (defense-in-depth quoting), and `StoreBusyError` is raised when the per-collection ingest lock cannot be acquired. **E0c**: `sample_chunk_texts` fetches rows in LanceDB insertion order, then shuffles them in-process via `random.shuffle` before returning, so callers receive a non-deterministic sample on each call. `list_documents(collection, limit, cursor)` gains a `cursor` parameter (opaque `doc_id`): results are sorted by `doc_id` ascending; a missing/deleted cursor silently resumes from the next sort position; returns `(items, next_cursor | None, total)` where `total` is the full document count via `count_documents` (independent of the current page). Namespace filtering is applied at the `pipeline.list_documents` layer, not in the store. **E2a**: `STORE_SCHEMA_VERSION` bumped `0` → `1`; two migrations registered with `introduced_at=1`: `migrate_expires_at_and_scopes` (adds `expires_at: utf8 \| null` and `scopes: list<utf8> \| null` to chunk tables) and `migrate_default_ttl_seconds` (adds `default_ttl_seconds: int64 \| null` to `_archon_collection_meta`). New methods: `prune_expired_chunks(collection, namespace) -> list[str]` (returns deduplicated doc_ids of pruned chunks), `count_expired_chunks(collection, namespace) -> int` (live count of expired chunks; spans all namespaces sharing the collection — not namespace-scoped), `query_expiring_chunks(collection, namespace, within_seconds, limit, cursor)` (cursor-paginated expiring-chunk list). | `SearchStore`, `SearchStore.hybrid_search_with_trace`, `SearchStore.sample_chunk_texts`, `SearchStore.list_documents`, `SearchStore.prune_expired_chunks`, `SearchStore.count_expired_chunks`, `SearchStore.query_expiring_chunks`, `StoreBusyError`, `validate_metadata`, `parse_metadata` |
| `archon_search/store_filters.py` | Single source of truth for SQL predicate building and literal quoting for LanceDB (DataFusion) `where`/`delete`/`count_rows` predicates; `build_where` compiles `SearchFilters` → SQL, `_sql_quote_str` is used by `store.py`'s `_where_eq`/`_where_in` as the defense-in-depth boundary behind upstream identifier regex gates. **E2a**: `build_where` gains `scope_filter: str \| None = None` parameter; `_where_list_has_or_null` helper added for exact scope-tag matching (`list_has(scopes, '<value>') OR scopes IS NULL`). | `_sql_quote_str`, `build_where`, `_compute_fetch`, `escape_like` |
| `archon_search/reranker.py` | Cross-encoder second-stage rerank façade with a Protocol backend. | `Reranker`, `RerankerBackend`, `ModelReranker`, `make_reranker` |

## Query

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/pipeline.py` | Orchestrates ingest (`ingest_file`, `ingest_directory`) and query (`search`, `search_with_context`, `explain`). **E0b**: `ingest_file()` unpacks `acl_list, warnings = resolve_acl(...)` and appends warnings to `IngestResult.warnings`. `SearchPipelineResult` gains `rag_fusion_warning: str | None` (set by the pipeline when RAG Fusion fails; HyDE warning assembled at route level). **D4**: `ingest_file()` emits chunks in batches of ≤ 512 (`_INGEST_CHUNK_BATCH_SIZE`). The first batch is embedded first (to initialise `embedder.embedding_dim`), then `ensure_collection` + `delete_document` run once; the batch loop then embeds remaining batches and calls `store.ingest_chunks()` for each (batch 0 reuses the pre-embedded vectors). `ingest_directory()` has no corpus-wide accumulators — per-file results are processed and discarded before the next file starts. Updates per-collection centroid incrementally per batch (B5 incremental path, unconditional since D4) and may trigger description regeneration via `store.sample_chunk_texts(n=100)` (O(1) memory). `explain` fetches an amplified candidate pool via `store.hybrid_search_with_trace`, ACL-filters, optionally reranks the entire pool via `_rerank_with_trace`, then splits top-k results from up to 20 near-misses. **C4**: `search`, `search_many`, and `search_with_context` accept `query_vector: list[float] \| None = None`; when provided, the ANN lookup uses this vector instead of embedding the query. **C5**: `search`, `search_many`, `search_with_context`, and `explain` gain `rag_fusion: bool`, `rag_fusion_generator`, and `rag_fusion_config` parameters. `_fuse_rag_fusion_results()` implements second-pass cross-variant RRF. `SearchPipelineResult` gains `rag_fusion_applied`, `rag_fusion_queries_used`, `rag_fusion_attempted`. `ExplainPipelineResult` gains those plus `rag_fusion_failure_reason` and `rag_fusion_sub_query_results`. **E0d**: `SearchPipeline.__init__()` gains `max_file_mb: int = 0` parameter (set from `cfg.ingest.max_file_mb` in both `create_pipeline()` and `app.py`). `ingest_file()` calls `_file_exceeds_limit(path, self._max_file_mb)` before the parse step and returns `IngestResult(status="error", code="file_too_large")` directly on rejection — no raise, preserving the soft-fail contract for batch/watcher callers. `create_pipeline()` passes `max_file_mb=cfg.ingest.max_file_mb`. **E1a**: `ingest_file()` calls `GraphExtractor.extract` and `GraphStore.write_graph` after successful chunk ingest when `graph.enabled = true` (post-ingest extraction hook); edge count ≥ `graph.backend_threshold_edges` → WARNING log + hint in `IngestResult.warnings`. `search()` and `search_many()` call `GraphExpander.expand(query, collection)` before embedding when `graph_mode="naive"` (pre-search expansion hook); `SearchPipelineResult` gains `graph_expansion_applied: bool`. **E1c**: `explain()` gains `graph_mode: Literal["naive", "local", "global"] \| None = None`; dispatches to `_explain_naive_graph_candidates` (naive mode: graph-expanded candidates with per-candidate `GraphProvenance`), `_explain_community_candidates` (local/global modes: community-representative candidates with `community_id` in `TraversalStep`), or the standard path when `graph_mode` is `None`. `_explain_merge_and_rank` merges graph candidates with baseline hybrid candidates, deduplicates, and reranks. `ExplainPipelineResult` gains `graph_mode_applied: Literal["naive", "local", "global"] \| None`. `GraphCommunitiesNotBuiltError` (defined in `pipeline.py`) is raised by `_explain_community_candidates` when communities have not been built for the collection. | `SearchPipeline`, `SearchPipelineResult`, `ExplainPipelineResult`, `ExplainStageError`, `RagFusionSubQueryInfo`, `SearchWithContextResult`, `create_pipeline`, `GraphCommunitiesNotBuiltError` |
| `archon_search/hyde.py` | **C4** — HyDE (Hypothetical Document Embeddings) query expansion. `HyDEGenerator` lazy-imports `anthropic`, applies a per-process token-bucket rate limiter, generates a short hypothetical answer passage via the Anthropic API, and embeds it with the local fastembed model. Raw query text is never logged — log messages use `_query_fingerprint` (SHA-256, 16-char hex) from `archon_search._privacy` for correlation. Falls back (`None` return) on timeout, API error, rate limit, missing key, or missing package. `resolve_hyde_vector` is the route-layer helper that applies the operator kill-switch (`config.hyde.enabled`) and translates the `(query, hyde, generator, config)` quadruple to `(vector \| None, hyde_applied: bool)`. | `HyDEGenerator`, `resolve_hyde_vector` |
| `archon_search/rag_fusion.py` | **C5** — RAG Fusion multi-query decomposition. `RAGFusionGenerator` lazy-imports `anthropic`, applies a per-process token-bucket rate limiter, calls the Anthropic API to generate N semantic query variants, and validates each variant (≤500 chars, no control sequences). Returns `[]` on timeout, API error, rate limit, missing key, or other failure; raises `RAGFusionDependencyError` if package not installed (→ route returns 422). Raw query text is never logged — log messages use `_query_fingerprint` from `archon_search._privacy`. | `RAGFusionGenerator`, `RAGFusionDependencyError` |
| `archon_search/_diagnostics.py` | Internal diagnostics types used by the explain path. `ScoredSearchCandidate` carries per-candidate score provenance plus ACL token list (`acl: list[str] \| None`) and A1/A2 metadata fields (`file_type`, `indexed_at`, `updated_at`, `ingested_by`, `language`, `metadata`). Not part of the public import surface. **E1c**: gains `TraversalStep` dataclass (`entity: str`, `entity_id: str`, `relationship: str \| None`, `community_id: str \| None`, `chunk_id: str \| None` — constraint-free at dataclass level; validation enforced by `TraversalStepResponse` model validator) and `GraphProvenance` dataclass (`steps: list[TraversalStep]`); `ScoredSearchCandidate` gains `graph_provenance: GraphProvenance \| None = None` (set by `_explain_naive_graph_candidates` / `_explain_community_candidates` in `pipeline.py`). | `ScoredSearchCandidate`, `SearchScoreBreakdown`, `TraversalStep`, `GraphProvenance` |
| `archon_search/model_validation.py` | **D6** — Provider/model validation shared by the install wizard and server startup. `validate_providers_shared(providers, embedding_model, reranker_model) -> (embedder_ok, reranker_ok, warnings)` is synchronous and never raises: it pre-flight-gates non-CPU ONNX providers against `onnxruntime.get_available_providers()`, then probes the embedder and reranker (skipping each when its model name is `""`). `validate_models_async(config, timeout_seconds=60, embedder_is_warm=False) -> ModelValidationResult` runs it under `asyncio.to_thread` + `asyncio.wait_for` (the timeout guarantee) and never raises — timeout, `CancelledError`, or any error yields a failure `ModelValidationResult` rather than propagating. `ModelValidationResult` is the DTO (`embedder_ok`/`reranker_ok` are `bool \| None`, null while pending; `provider_warnings`; `validated_at`). The pre-existing `validate_embedding_model()` (wizard dimension lookup) and `ModelValidationError` are unchanged. | `ModelValidationResult`, `validate_providers_shared`, `validate_models_async`, `validate_embedding_model`, `ModelValidationError` |

## Graph (E1a / E2g)

**Table naming (E2b):** `_archon_graph_{ns}__{col}_nodes|edges|communities|mentions` (double `__` between namespace and collection). Pre-namespacing `_archon_graph_{col}_*` tables are orphans after upgrade — a startup WARNING lists them.

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/graph_types.py` | Entity/edge domain dataclasses and deterministic ID helpers. `GraphNode` and `GraphEdge` are the canonical entity and relationship types. `GraphExtractionResult` is the per-document extraction output. `ChunkInput` is the light DTO passed from the pipeline to the extractor. `make_stable_entity_id(entity_type, entity_name)` and `make_stable_edge_id(src_id, rel, tgt_id)` produce reproducible SHA-256-derived IDs so re-ingesting the same document is idempotent. **E2g**: `RelationshipType` gains `calls`, `imports`, `defines`, `inherits`; `ImpactResult`, `ImpactGroup`, `ImpactEdge` dataclasses for `compute_impact` traversal results; constants `DEFAULT_IMPACT_DEPTH=2`, `MAX_IMPACT_DEPTH=5`, `MAX_IMPACT_GROUP_SIZE=50`; `make_code_symbol_qualified_name` helper for file-qualified `code_symbol` node IDs. | `GraphNode`, `GraphEdge`, `GraphExtractionResult`, `ChunkInput`, `make_stable_entity_id`, `make_stable_edge_id`, `ImpactResult`, `ImpactGroup`, `ImpactEdge` |
| `archon_search/graph_store.py` | Frameworks & Drivers layer — LanceDB-backed graph persistence. Table-naming scheme: `_archon_graph_{ns}__{col}_nodes|edges|communities|mentions`. Key methods: `ensure_graph_tables(collection, ns)` (idempotent schema creation), `write_graph(collection, nodes, edges, ns)` (upsert nodes/edges — tag-collision guard: incoming `"inferred"` edges whose stored counterpart is `"extracted"` are promoted back to `"extracted"` before the bulk upsert), `get_neighbours(collection, entity_ids, ns)`, `get_edges_for_nodes(collection, entity_ids, ns)`, `find_nodes_by_name(collection, names, ns)`. **E2g**: `compute_impact(collection, symbol, depth, direction, extraction_method_filter, file_path, ns) -> ImpactResult` (BFS traversal, depth/group-size capped, PageRank-ordered), `write_pagerank_scores(collection, scores, ns)`, `pagerank_score(collection, entity_id, ns) -> float | None`. **E2h**: `get_mentions_for_entity_ids(collection, entity_ids, ns) -> list[GraphMention]` — returns mention rows for the supplied entity IDs, preserving duplicates (needed for PPR personalization weighting where mention-row count encodes entity salience). `ns` is always the LAST positional parameter on every public method. | `GraphStore` |
| `archon_search/graph_extractor.py` | Interface Adapters layer — entity and relationship extraction from text. `GraphExtractor` wraps spaCy NER (dispatched to a thread via `asyncio.to_thread`). For `code_symbol` chunks (produced by C3 code enricher), dispatches to the path that produces a single `code_symbol` node from the chunk's metadata — no spaCy NER is run on code. Includes an LLM stub (`_llm_extract`) for future provider integration. Gracefully degrades when spaCy model is absent. | `GraphExtractor` |
| `archon_search/defref_extractor.py` | Interface Adapters layer — **E2g BE-2/BE-4/BE-5**: AST-aware def/ref edge extraction for code files. `DefRefExtractor` uses tree-sitter grammars (9 languages: Python, TypeScript, JavaScript, Go, Rust, Java, Bash, Swift, C#) to extract `calls`/`imports`/`defines`/`inherits` edges. Same-file edges are tagged `extraction_method="extracted"` (proven); cross-file name-based matches are tagged `"inferred"` (best-guess). `code_symbol` node IDs are file-qualified (name + `source_path` hashed via `make_stable_entity_id`) so same-named symbols in different files remain distinct graph nodes; `GraphNode.entity_name` stays the bare symbol name (never path-qualified). Depends on `GraphStoreProtocol` (constructor injection). | `DefRefExtractor` |
| `archon_search/pagerank_builder.py` | Use Cases layer — **E2g BE-7**: background PageRank computation over code-symbol edges. `PageRankBuilder.build(collection, ns)` builds an igraph graph from `GraphStore.get_all_edges()`, runs PageRank (unweighted), and writes results back via `GraphStore.write_pagerank_scores`. Triggered by `MaintenanceLoop`'s debounce after new edges are written; uses `asyncio.to_thread` mirroring `CommunityBuilder`. | `PageRankBuilder` |
| `archon_search/ppr_walker.py` | Use Cases layer — **E2h BE-5**: Personalised PageRank (PPR) retrieval for `graph_mode="ppr"`. `PPRWalker.walk(query, collection, damping, top_entities, ns) -> PPRWalkResult` tokenises the query into N-grams, looks up matched entity nodes via `find_nodes_by_name`, builds a personalization vector from `get_mentions_for_entity_ids` row counts, runs `networkx.pagerank` in `asyncio.to_thread` (CPU-bound), and resolves the top-K entities to chunk IDs in PPR rank order. Falls back to empty result (triggering hybrid fallback in the pipeline) when no entities match or no mention rows exist. Privacy invariant: raw query string is never logged. | `PPRWalker`, `PPRWalkResult` |
| `archon_search/graph_expander.py` | Use Cases layer — query expansion using the graph. `GraphExpander.expand(query, collection) -> ExpandedQuery` performs exact case-insensitive n-gram matching of query tokens against known graph nodes, calls `GraphStore.get_neighbours` for each matched entity, and returns the original query plus deduplicated first-degree neighbour entity names. Used by `SearchPipeline` when `graph_mode="naive"`. | `GraphExpander`, `ExpandedQuery` |
| `archon_search/community_builder.py` | Use Cases layer — Leiden community detection over the entity graph. `CommunityBuilder` lazy-imports `leidenalg`/`igraph`, clusters the node/edge tables into communities, applies recursive splitting when a community exceeds `max_community_size`, selects MMR representative chunks via `SearchStore`, and optionally calls the configured `extraction_model` for an abstractive summary. Key method: `build(collection, ns) -> list[Community]`. Output is persisted to `_archon_graph_{ns}__{col}_communities` by `GraphStore`. | `CommunityBuilder` |
| `archon_search/graph_store_protocol.py` | Use Cases layer — structural typing protocol (`GraphStoreProtocol`) that defines the minimum graph-storage interface required by Use Cases. Both `SynonymDetector`, `AliasLoader`, and `DefRefExtractor` depend on this protocol, not on the concrete `GraphStore`. Decorated `@runtime_checkable`. | `GraphStoreProtocol` |
| `archon_search/synonym_detector.py` | Use Cases layer — embedding-based synonym detection. **E2f**: `SynonymDetector.detect(collection, ns, skip_pairs) -> list[GraphEdge]` groups entity nodes by type, computes cosine similarity over stored `name_embedding` vectors (Python-side all-pairs; no ANN index required), and returns `synonym_of` edges for pairs above `config.graph.synonym_threshold` (default 0.85). Cross-type pairs and pairs in `skip_pairs` are always excluded. Returns `[]` when no nodes have embeddings. Depends on `GraphStoreProtocol` and `Embedder`. | `SynonymDetector` |
| `archon_search/alias_loader.py` | Use Cases layer — manual synonym alias file loader. **E2f**: `AliasLoader.load(collection, ns) -> tuple[list[GraphEdge], set[tuple[str, str]]]` reads a TOML file at `config.graph.alias_file`, resolves alias names via `graph_store.find_nodes_by_name()`, and returns edges with `extraction_method="manual"` plus a `skip_pairs` set for `SynonymDetector`. Missing or unreadable file logs WARNING and returns `([], set())`. Depends on `GraphStoreProtocol`. | `AliasLoader` |
| `archon_search/cli/graph_cmd.py` | Frameworks & Drivers layer — Click command group `graph` with subcommand `build-communities <collection>`. Loads config, connects `GraphStore` and `SearchStore`, instantiates `CommunityBuilder`, calls `build()`, and prints a community-count summary. Wired into `cli/main.py` as the `graph` group. | `graph`, `build_communities` |

## Routing

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/router.py` | Fetch collection metadata over JSON-RPC, score each centroid against the query embedding, apply a confidence gate, build a three-tier shortlist for the decomposer. **B4**: accepts `strategy: Literal["centroid", "hybrid"]` and `description_weight: float` constructor parameters. Under `strategy="hybrid"`, blends centroid cosine with `description_embedding` cosine: `score = (1 - w) * centroid_score + w * description_score`; falls back to pure centroid for collections with missing or dimensionally-inconsistent `description_embedding`. `_score_collections` is the shared scoring helper (extracted for A4); `rank_with_scores` returns every supplied collection paired with its centroid similarity, bypassing the confidence-threshold gate — used exclusively by `/explain`. Accepts `initial_metadata` for constructor-time injection; exposes `invalidate()` to clear the cached metadata for long-lived router instances. | `MultiCollectionRouter`, `MultiCollectionRouter.rank`, `MultiCollectionRouter.rank_with_scores`, `MultiCollectionRouter._score_collections`, `MultiCollectionRouter.invalidate` |

## Metadata

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/collection_meta.py` | Dataclass for per-collection metadata: name, centroid, description, doc/chunk counts, embedding model, timestamps, namespace. **B4**: adds `description_embedding: list[float] \| None = None`. **C1**: adds `active_embedding_model: str`, `pending_embedding_model: str \| None`, `needs_reindex: bool`, `reindex_job_id: str \| None` — the four fields that power the per-collection model state machine. **E2a**: adds `default_ttl_seconds: int \| None = None` — collection-level ingest TTL default (forward-only: affects new chunks only; set via `PATCH /collections/{name}`). | `CollectionMeta` |
| `archon_search/description_generator.py` | Decide whether to regenerate a collection description and call the model (Haiku) to generate one. **E0c**: `MAX_SAMPLE_CHUNKS` raised from 20 to 100 — description generation now draws from a shuffled window of up to 100 chunks fetched in insertion order (order is randomized; membership is still bounded to the first 100 rows). | `generate_description`, `_should_regenerate` (semi-public; gating heuristic), `MAX_SAMPLE_CHUNKS` (public module-level constant; pool size for description generation sampling, now 100) |

## Server

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/server/app.py` | FastAPI app factory. Wires `SearchPipeline`, `JobStore`, `SearchStore`, telemetry, CORS, BearerAuth. Lifespan runs store migrations on startup, drains telemetry on shutdown. **D6** — lifespan sets `app.state.model_validation = None`, then spawns `validate_models_async()` (reading `app.state.embedder.is_warm` for `embedder_is_warm`) as a non-blocking background task tracked in `app.state._background_tasks`; the wrapper catches `BaseException` and stores a failure `ModelValidationResult` if the task escapes. **D7** — `create_app()` instantiates `KeyStore(get_data_dir() / "keys.json")`, loads TOML `[namespaces]` synthetic records into it, passes `key_store=key_store` to `APIKeyMiddleware`, stores it on `app.state.key_store`, and includes `keys_router`. **E0c** — startup drift assertion (`assert _FANOUT_VALIDATION_LIMIT == SearchConfig().max_fanout`) removed; constant `_FANOUT_VALIDATION_LIMIT` no longer exists in `routes_search.py`. | `create_app`, `run_server` |
| `archon_search/server/mcp.py` | FastMCP app factory exposing pipeline-shaped tools over MCP HTTP, sharing `APIKeyMiddleware`. Every tool validates its return value through a Pydantic schema from `mcp_schemas.py` before serialization; schema drift returns `{"error": "...", "code": "schema_validation_error"}`. **D7**: `create_mcp_http_app()` gains `key_store: KeyStore \| None = None` parameter; wires it into `APIKeyMiddleware`. Registers four new MCP tools: `create_key`, `list_keys`, `revoke_key`, `rotate_key`. **D9**: `create_mcp_http_app()` is now invoked from `create_app()`'s lifespan and the returned Starlette app is mounted at `/mcp` on the shipped server (no longer test-only). It now passes `config.namespaces` into its `APIKeyMiddleware` (previously hardcoded `{}`) — `namespaces` is extracted from the `config` argument inside the factory, not a separate parameter — plus the lifespan-constructed `writer` and `key_store`; with `key_store` present, all 17 MCP tools register. Tool closures recover the per-request namespace via `_get_request_namespace()` (FastMCP `get_http_request()` -> `request.state.namespace` set by `APIKeyMiddleware`). See [ADR 09](../ADRs/09_mcp_http_mount_and_namespace_propagation.md). **E0c**: `_FANOUT_VALIDATION_LIMIT` import and all uses removed; `search` and `explain` tool closures now read `config.max_fanout` directly; `explain` tool also checks `config.top_k_max`. `list_documents` tool gains optional `cursor: str \| None = None` parameter (additive). **E1a**: `search` tool gains `graph_mode: str | None = None`; `search_with_context` returns `{"error": "...", "code": "graph_mode_not_supported"}` when `graph_mode` is non-null. **E1c**: `explain` tool gains `graph_mode: str \| None = None` (validated against `_VALID_GRAPH_MODES`); passes through to `pipeline.explain()`; result dict includes `graph_mode_applied` and per-result `graph_provenance` via `ExplainResponse.model_dump(mode="json", exclude_none=False)`. **E2a**: `ingest_file` and `ingest_directory` gain `chunk_ttl_seconds: int \| None` and `chunk_scopes: list[str] \| None`; `search`, `search_with_context`, `explain` gain `scope_filter: str \| None`. | `create_app`, `create_mcp_http_app`, `_ERR_SCHEMA` |
| `archon_search/server/mcp_schemas.py` | MCP-specific Pydantic schemas for the **12** retrieval, collection, document, and export tool return shapes (not all 17 MCP tools use a dedicated Pydantic schema) (`extra='forbid'`). `from_result()` classmethods map domain dataclasses to public contract types, excluding transient/internal fields (`vector`, `start_offset`, `end_offset`, `custom_score`, `centroid`, `centroid_sum`, `needs_recompute`, `needs_reindex`, `reindex_job_id`, `namespace`, `mutations_since_recompute`, `described_at_doc_count`). `active_embedding_model` is exposed as `embedding_model` in all collection schemas. **E0b**: `McpSearchResponse` and `SearchWithContextResponse` gain `expansion_used: bool` and `expansion_warning: str | None`. `IngestResultSchema` gains `warnings: list[str]`. **E0d**: `IngestResultSchema` gains `code: Literal["file_too_large"] \| None = None`; `from_result()` maps it from `IngestResult.code`. MCP `ingest_file` tool now surfaces `code="file_too_large"` in its result dict when the size guard fires. | `McpSearchResultSchema`, `McpSearchResponse`, `ExcludedCollectionMcpSchema`, `ContextChunkSchema`, `SearchWithContextItemSchema`, `SearchWithContextResponse`, `CollectionListItemSchema`, `CollectionDetailSchema`, `CollectionMetaMcpSchema`, `IngestResultSchema`, `DocumentInfoSchema`, `DeleteDocumentSchema` |
| `archon_search/server/routes_keys.py` | **D7** — `POST /keys`, `GET /keys`, `DELETE /keys/{id}`, `POST /keys/rotate`. Issue, list, revoke, and rotate managed API keys. `POST /keys/rotate` writes the new raw token to `.search.env` via `atomic_write_bytes` and delegates key-record mutation to `KeyStore.rotate_default_key()`. Returns `409` when `ARCHON_SEARCH_API_KEY` env var is set. | `router` |
| `archon_search/server/middleware_auth.py` | Bearer-token middleware; resolves namespace per request; constant-time key comparison; exempts `/health`, `/ready`, `/docs`, `/openapi.json`, `/redoc`. **D7**: gains additive `key_store: KeyStore \| None = None` parameter; when present, checks managed keys (SHA-256 `hmac.compare_digest`, early-exit on match) before the TOML namespace loop and the legacy `api_key` fallback. Adds rotation-revocation guard: before accepting via legacy path, checks whether the token hash matches a revoked/expired managed key record. | `APIKeyMiddleware`, `_EXEMPT_PATHS` |
| `archon_search/server/routes_health.py` | `GET /health`. **D9** — imports `_build_mcp_status` from `routes_status` and includes `mcp: McpStatusDetail \| None` in the response (`None` when MCP disabled or unmounted). | `router`, `health` |
| `archon_search/server/routes_state.py` | `GET /indexing-state`. | `router` |
| `archon_search/server/routes_status.py` | `GET /status`. **D6** — reads `app.state.model_validation` (via `getattr` guard) and includes `model_validation: ModelValidationStatus \| None` in the response (`None` while pending). **D9** — `_build_mcp_status()` reads `app.state.mcp_bound` and `config.mcp.enabled` to populate `mcp: McpStatusDetail \| None` (also reused by `routes_health.py`); `bind_address = f"{config.host}:{config.port}/mcp"` when bound, else `None`. **E0c** — `_build_search_status(config)` populates `search: SearchStatusDetail` from the live `SearchConfig` (`max_fanout`, `top_k_max`); always non-null when config is present. **E1a** — `_build_graph_status(config, store)` populates `graph: GraphStatusDetail | None` (null when `graph.enabled = false`; when enabled, queries `GraphStore` for per-collection node/edge counts). | `router`, `_build_mcp_status`, `_build_search_status`, `_build_graph_status` |
| `archon_search/server/routes_ready.py` | `GET /ready` (B2) — unauthenticated readiness probe; `ready: bool` is storage-only (`SearchStore.ping()`). **D6** — `_model_check_status` maps `app.state.model_validation` to `checks.models: CheckStatus` with strict priority FAIL > WARN > OK (and PENDING while unset); the models check never affects `ready` or the HTTP status. | `router` |
| `archon_search/server/routes_search.py` | `POST /search`. **E0c** — `_FANOUT_VALIDATION_LIMIT` constant removed; fanout count (`len(collections) > config.max_fanout`) and `top_k` upper-bound (`top_k > config.top_k_max`) checks moved to the route handler body, reading live `SearchConfig` values. `SearchRequest.top_k` no longer carries `le=100` (static bound removed); the `Field(ge=1)` lower-bound constraint remains. 422 error shape changes from Pydantic list to handler string for these two conditions (see `BREAKING.md`). **E1a**: `SearchRequest` gains `graph_mode: Literal["naive"] | None = None`; when `"naive"` and `graph.enabled = true`, `GraphExpander` expands the query before embedding; returns `422` when `graph_mode="naive"` and `graph.enabled = false`. `SearchResponse` gains `graph_expansion_applied: bool`. **E2a**: `SearchRequest` gains `scope_filter: str \| None`; handler validates syntax (400 on bare/leading/mid-string `*`) and mutual exclusion with `graph_mode` (422). | `router`, `SearchRequest`, `SearchResponse`, `SearchResultSchema` |
| `archon_search/server/routes_route.py` | `POST /route` — driver for `MultiCollectionRouter`. Defines a route-local Pydantic `RouteResponse` distinct from the dataclass `RouteResponse` in `archon_search/types.py` (two public objects share the name). | `router`, `RouteRequest`, `RouteResponse` (Pydantic) |
| `archon_search/server/routes_collections.py` | `GET/POST /collections/`, `GET/DELETE/PATCH /collections/{name}`, `POST /collections/{name}/reindex`. Issues jobs for add, reindex, and delete (non-empty collections). **C1**: `PATCH /collections/{name}` implements the per-collection embedding model state machine; `POST /collections/` gains optional `embedding_model` field. **E0c**: adds `GET /collections/{name}/documents` — cursor-paginated document listing; `limit` (1–200, default 50) and `cursor` declared as FastAPI `Query` params (standard Pydantic 422 on out-of-range); delegates to `pipeline.list_documents`. **E2a**: new `GET /collections/{name}/expiring?within_hours={n}` endpoint; `PATCH` gains `default_ttl_seconds`; document listing includes `scopes` per document. | `router`, `AddCollectionRequest`, `PatchCollectionBody` |
| `archon_search/server/routes_jobs.py` | `POST /ingest`, `GET /jobs/{id}`, `DELETE /jobs/{id}`. Spawns the ingest task against `SearchPipeline`. **E0d**: `POST /ingest` adds a synchronous 413 pre-check (`_file_exceeds_limit` from `_types.py`) placed BEFORE `job_store.create()` — only fires when `body.path` refers to a single file (not a directory, not a `documents` payload) and `config.ingest.max_file_mb > 0`. Returns `HTTPException(413)` immediately; no job is created. **E2a**: `IngestRequest` gains `chunk_ttl_seconds: int \| None` and `chunk_scopes: list[str] \| None`. | `router`, `IngestRequest` |
| `archon_search/server/routes_explain.py` | `POST /explain` (A4). All schemas use `extra="forbid"`. Public Pydantic models: `ExplainRequest`, `ExplainResponse`, `ExplainResult`, `ExplainNearMiss` (no `text` field — structurally absent), `ExplainScoreBreakdown`, `RoutingExplain`, `RoutingCandidate`. `ExplainResponse.from_pipeline_result` is the seam between private `ScoredSearchCandidate` and the public wire schema. **E0c**: `ExplainRequest.top_k` static `le=100` bound removed; `top_k > config.top_k_max` checked in handler body (same pattern as `routes_search.py`). Fanout check also moved to handler body. **E1c**: `ExplainRequest` gains `graph_mode: Literal["naive", "local", "global"] \| None = None`. `ExplainResponse` gains `graph_mode_applied: Literal["naive", "local", "global"] \| None`. `ExplainResult` gains `graph_provenance: GraphProvenanceResponse \| None` (populated for graph-retrieved results, `null` for standard hybrid results). `ExplainNearMiss` structurally omits `graph_provenance`. New models: `TraversalStepResponse` (model validator enforces at least one of `relationship`, `community_id`, or `chunk_id` is set) and `GraphProvenanceResponse` (wraps `list[TraversalStepResponse]`). Two new pre-pipeline 422 guards: (1) `graph_mode` non-null with `graph.enabled = false` → plain string detail; (2) `graph_mode` non-null with `collections` (multi-collection) → plain string detail. `GraphCommunitiesNotBuiltError` caught at route level → `{"detail": {"code": "graph_communities_not_built", "message": "..."}}` (422). **E2a**: `ExplainRequest` gains `scope_filter: str \| None` with same validation as search. | `router`, `ExplainRequest`, `ExplainResponse`, `ExplainResult`, `ExplainNearMiss`, `ExplainScoreBreakdown`, `RoutingExplain`, `RoutingCandidate`, `TraversalStepResponse`, `GraphProvenanceResponse` |
| `archon_search/server/_ingest_lock.py` | Shared helper for `POST /ingest` and `POST /collections/` to pre-acquire the per-collection ingest lock and return a `503` + `Retry-After` (`{"error": "store_busy", ...}`) when a reindex holds it. | `acquire_collection_lock_or_503` |
| `archon_search/server/routes_openai_shim.py` | **G9** — `GET /v1/models` and `POST /v1/chat/completions` OpenAI-compatible shim. Conditionally registered in `create_app()` when `[openai_shim] enabled = true`. `GET /models` returns one `ModelObject` per namespace-visible collection plus the `archon-search` catch-all. `POST /chat/completions` extracts the last `role="user"` message as a query; `model="archon-search"` fans out across all namespace collections (capped at `config.max_fanout`); `model="archon-search/{col}"` targets one collection directly. Streaming (`stream=true`) materialises retrieval results first, then returns a `StreamingResponse` yielding SSE `data:` frames (one per chunk, then a stop frame, then `data: [DONE]`). `OpenAI401Middleware` rewrites bodyless 401 responses on `/v1/*` paths to OpenAI JSON error shape. | `router`, `OpenAI401Middleware`, `chat_completions`, `list_models` |
| `archon_search/server/schemas_openai.py` | **G9** — OpenAI-shaped Pydantic models for the shim: `ModelObject` (`id`, `object="model"`, `created=0`, `owned_by="archon-search"`), `ModelList`, `OpenAIError` (`message`, `type`, `param?`, `code?`), `OpenAIErrorResponse` (`error: OpenAIError`), `ChatMessage` (`role`, `content: str` — array content deliberately unsupported), `ChatCompletionRequest` (`model`, `messages`, `stream=False`, `top_k?`), `ChatCompletionChoice`, `ChatCompletionUsage` (always zeros), `ChatCompletionResponse`, `ChunkDelta`, `ChatCompletionChunkChoice`, `ChatCompletionChunk`. | `ModelObject`, `ModelList`, `OpenAIError`, `OpenAIErrorResponse`, `ChatMessage`, `ChatCompletionRequest`, `ChatCompletionResponse`, `ChatCompletionChunk` |
| `archon_search/server/routes_telemetry.py` | `GET /telemetry/stats`, `GET /telemetry/entries`. | `router` |
| `archon_search/server/routes_backup.py` | **D2** — `POST /backup/trigger`. Enumerates the caller's namespace collections, dedups against `BackupLoop._in_flight` and `JobStore.list_queued_bulk()`, then enqueues backup-sourced `ExportJob`s; returns `BackupTriggerResponse` with `queued` job IDs and `skipped` reasons. | `router`, `BackupTriggerResponse`, `SkippedItem` |
| `archon_search/server/routes_maintenance.py` | **D5** — `POST /maintenance/trigger`. Sets `MaintenanceLoop._trigger_event` for an immediate on-demand pass; returns `{"status": "triggered"}` (202) or `{"status": "already_triggered"}` (202) when a pass is already pending or running. | `router`, `MaintenanceTriggerResponse` |
| `archon_search/server/schemas.py` | REST response models: `HealthResponse`, `StatusCollectionEntry`, `StatusResponse` (D5: gains `maintenance: MaintenanceStatusDetail \| None`; **D6**: gains `model_validation: ModelValidationStatus \| None`), `IndexingStateCollectionEntry`, `IndexingStateResponse`, `CollectionSummary`, `CollectionDetail`, `JobResponse`, `DeleteResponse`, `ErrorDetail`. **D5** adds `MaintenanceStatusDetail`, `CollectionHealthEntry`, `MaintenanceTriggerResponse`. **D6** adds `ModelValidationStatus`, extends `CheckStatus` with `PENDING`/`WARN`, and adds `models: CheckStatus = CheckStatus.PENDING` to `ReadinessChecks`. **D7** adds `KeyCreateRequest`, `KeyCreateResponse`, `KeyResponse`, `KeyListResponse`, `KeyRevokeResponse`, `KeyRotateRequest`, `KeyRotateResponse`. **D9** adds `McpStatusDetail` (`enabled: bool`, `bind_address: str \| None` serialised as camelCase `bindAddress`) and an `mcp: McpStatusDetail \| None` field on both `StatusResponse` and `HealthResponse`. **E0b** adds `HydeStatusDetail` (`key_available: bool`) and `RagFusionStatusDetail` (`key_available: bool`); `StatusResponse` gains `hyde: HydeStatusDetail \| None` (null when `[hyde].enabled = false`), `rag_fusion: RagFusionStatusDetail \| None` (null when `[rag_fusion].enabled = false`), and `failed_expired_ingest_count: int = 0`. **E0c** adds `SearchStatusDetail` (`max_fanout: int`, `top_k_max: int`); `StatusResponse` gains `search: SearchStatusDetail \| None = None`. Adds `DocumentInfoItem` (`doc_id`, `source_path`, `chunk_count`, `indexed_at`) and `DocumentListResponse` (`items: list[DocumentInfoItem]`, `next_cursor: str \| None`, `total: int`) for the new `GET /collections/{name}/documents` endpoint. **E1a** adds `GraphStatusDetail` (`enabled: bool`, `node_count: int`, `edge_count: int`, `collections: list[GraphCollectionStats]`) and `GraphCollectionStats` (`collection: str`, `node_count: int`, `edge_count: int`); `StatusResponse` gains `graph: GraphStatusDetail \| None` (null when `[graph].enabled = false`). **E2a**: `MaintenanceStatusDetail` gains `expired_chunk_count: int` and `last_expired_pruned_at: str \| null`; new `ExpiringChunkItem` and `ExpiringChunksResponse`; `DocumentInfoItem` gains `scopes: list[str]`; `PatchCollectionBody` gains `default_ttl_seconds: int \| null`. | (Pydantic models) |
| `archon_search/server/schemas_telemetry.py` | Pydantic models for telemetry routes. **E0b**: `StatsResponse` gains `truncated_count: int = 0` — count of entries where `truncated=True`. | see source: `archon_search/server/schemas_telemetry.py` |
| `archon_search/server/graph_viewer.html` | **E2j** — Self-contained force-directed graph viewer page served by `GET /graph/{collection}/view`. All JS (vis-network 9.1.9) and CSS are inlined; no external CDN dependencies. Fetches `GET /graph/{collection}` using the embedded Bearer token, renders nodes sized by `salience` and colored by `entity_type`, edges with thickness proportional to `weight`. Node click opens a side panel showing `entity_name`, `entity_type`, `chunk_count`, and co-occurrence chunk IDs. Text search filters visible nodes case-insensitively. Shows a truncation banner when `truncated=true` and an empty-state message when `nodes` is empty. Four `__ARCHON_*__` placeholders are substituted server-side by `routes_graph.get_graph_view`. | (static HTML; no public Python symbols) |

## CLI

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/cli/main.py` | `archon-search` Click group; registers subcommands: `start`, `stop`, `status`, `serve`, `install`, `uninstall`, `ingest`, `sync`, `collection`, `config`, `export`, `import`, `backup`, `maintenance` (D5), `key` (D7). | `main` |
| `archon_search/cli/start.py` | `start` subcommand; validates config, delegates to `launchctl` / `systemctl --user`. | see source |
| `archon_search/cli/stop.py` | `stop` subcommand. | see source |
| `archon_search/cli/status.py` | `status` subcommand; hits the HTTP control plane. | see source |
| `archon_search/cli/serve.py` | **C9** — `serve` subcommand; foreground-blocking uvicorn launcher used by the Docker image and any direct-run topology where launchd/systemd are unwanted. Calls `load_config(path, serve=True)` (host defaults to `0.0.0.0`) then `run_server(config)`. Never touches `_get_service()`. Emits a startup warning when `ARCHON_SEARCH_DATA_DIR` is set without `ARCHON_SEARCH_CONFIG`. | `serve` |
| `archon_search/cli/install_cmd.py` | `install` / `uninstall` subcommands; wraps `archon_search/install.py` and the `platform/` lifecycle. | see source |
| `archon_search/cli/ingest.py` | `ingest` subcommand; submits an ingest job. **E0d**: `--path` now routes to `pipeline.ingest_file()` when the path is a single file (collection name = `Path(path).stem`); directory paths still route to `pipeline.ingest_directory()`. `--path` help text updated to "File or directory to ingest". Prints a pre-parse notice to stderr for any file > 10 MB (`_LARGE_FILE_NOTICE_MB = 10`): "Parsing large file (X MB); this may take a while…". When `pipeline.ingest_file()` returns an error `IngestResult` (including the `code="file_too_large"` case from the size guard), prints `result.error` to stderr and exits non-zero via the generic error path. | see source |
| `archon_search/cli/sync.py` | `sync` subcommand; drives `SearchCollectionSync`. | see source |
| `archon_search/cli/collection.py` | `collection` subgroup; list/add/remove/info/reindex. | see source |
| `archon_search/cli/config_cmd.py` | `config` subgroup; show/edit `~/.archon-search/archon-search.toml`. | see source |
| `archon_search/cli/backup_cmd.py` | **D2** — `backup` Click group. Bare invocation prints help. `--now` POSTs `/backup/trigger` and prints queued job IDs and skipped collections; `--wait` polls each job to terminal state. `backup status` subcommand reads `.backup-state.json` offline, counts archives on disk, and merges `last_tick_at`/`next_run_at` from `GET /status` when reachable; `--json` emits a `BackupStatusDetail`-shaped payload. | `backup_cmd` |
| `archon_search/cli/maintenance_cmd.py` | **D5** — `maintenance` Click group. `status` subcommand reads `.maintenance-state.json` offline (S25) and optionally merges live `maintenance` block from `GET /status` when server is reachable; `--json` flag. `run` subcommand POSTs `/maintenance/trigger` and exits immediately; `--wait` polls `GET /status` until `maintenance.last_run_at` changes (S26, S27). | `maintenance_cmd` |
| `archon_search/cli/key_cmd.py` | **D7** — `key` Click group. `create` issues a new managed API key via `POST /keys` (raw token to stdout only, banner to stderr). `list` prints active keys via `GET /keys`; `--status all` shows all; hint line for hidden revoked count. `revoke <id>` calls `DELETE /keys/{id}`. `rotate` calls `POST /keys/rotate`; `--grace DURATION` sets the old-key expiry grace period. Duration strings: `30d`, `12h`, `3600s`, or ISO-8601 datetime with timezone offset. | `key_cmd` |
| `archon_search/cli/_helpers.py` | Shared CLI infrastructure (auth header, base URL resolution, error printing). | internal |

## Platform

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/platform/runtime.py` | Resolve binary paths and runtime context. | `SearchRuntime`, `get_runtime`, `find_binary` |
| `archon_search/platform/service.py` | Abstract service lifecycle. | `SearchServiceLifecycle`, `ServiceStatus` |
| `archon_search/platform/macos.py` | launchd implementation. | `LaunchdSearchService` |
| `archon_search/platform/linux.py` | systemd implementation. | `SystemdSearchService` |
| `archon_search/platform/windows.py` | Windows service implementation. | `WindowsSearchService` |
| `archon_search/platform/types.py` | Platform-shared types (currently the `GpuType` enum). | `GpuType` |

## Telemetry

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/telemetry/entry.py` | Structural model + factories. **Factories take no `query` parameter — raw queries are never logged.** `EndpointKind` includes `explain` (A4); `from_explain_result` records `collection` and `result_count` (scalar) with no query text or result doc IDs. | `TelemetryEntry`, `EndpointKind`, `Status`, `ErrorKind` |
| `archon_search/telemetry/writer.py` | Background JSONL writer; one line per call into `~/.archon-search/search-logs/` via a persistent per-date fd with rotate-only fsync (not per-line fsync; see [130 durability contract](130_data_architecture_and_persistence.md#telemetry-durability-rotate-only-fsync)). | `TelemetryWriter` |
| `archon_search/telemetry/reader.py` | Reads logs for `/telemetry/stats` and `/telemetry/entries`. | `TelemetryReader` |
| `archon_search/telemetry/pruner.py` | Enforces `retention_days` on the log directory. | `Pruner` |
| `archon_search/telemetry/hasher.py` | **D8** — HMAC-SHA256 doc_id hasher. `hash_doc_id(salt, doc_id) -> str` produces a deterministic 64-char lowercase hex digest; `load_or_create_salt(enabled) -> bytes \| None` reads/creates `get_data_dir()/.telemetry-salt` (mode 0600). Spans Entities (pure function) + Frameworks & Drivers (file I/O), like `key_manager.py`. | `hash_doc_id`, `load_or_create_salt` |

## Jobs

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/jobs/store.py` | Persistent JSON-backed job store with atomic writes, 7-day eviction, RUNNING/CANCELLING crash-to-FAILED recovery, and `transition()` for atomic state changes. | `JobStore` |
| `archon_search/jobs/model.py` | Re-exports `IngestJob`/`JobStatus`, exposes `get_jobs_file()` (lazy, honours `ARCHON_SEARCH_DATA_DIR`), provides `job_to_dict` for response shaping. | `IngestJob`, `JobStatus`, `get_jobs_file`, `job_to_dict` |
| `archon_search/jobs/backup_loop.py` | **D2** — In-process scheduled-backup orchestrator. Two async loops (`_trigger_loop` enumerates collections per `interval_hours` and enqueues `ExportJob` with `source="backup"`; `_completion_loop` polls in-flight jobs, persists `last_backup_at` to `.backup-state.json`, and rotates archives per `keep`). Run together via `asyncio.gather`. | `BackupLoop` |
| `archon_search/jobs/maintenance_loop.py` | **D5** — In-process maintenance orchestrator. Single `_trigger_loop` waits on `asyncio.wait_for(_trigger_event.wait(), timeout=interval_seconds)` (or indefinitely when `interval_hours=0`); fires `_run_one_pass` on timeout or trigger. Each pass: iterates all collections via `store.list_collections()`, skips excludes, runs `_run_fts_optimize` + `_run_orphan_cleanup` per-collection, then calls `_run_failed_ingest_retry` once at the pass level; writes `.maintenance-state.json` atomically after the pass. **E0b**: `_run_failed_ingest_retry` transitions aged-out FAILED `IngestJob`s to `FAILED_EXPIRED` (age ≥ `retry_max_age_hours` OR `retry_count >= retry_max_attempts`) via `job_store.transition()` instead of silently discarding them. | `MaintenanceLoop` |

## Eval

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/eval/backends.py` | Deterministic, corpus-aware but label-blind embedder + reranker so eval metrics are stable without real model weights. | `EvalEmbedderBackend`, `EvalRerankerBackend` |
| `archon_search/eval/runner.py` | Loads thresholds, runtime config, baselines; emits the eval report. | `EvalThresholds`, `EvalQualityFloors`, `EvalLatencyCeilings`, `EvalRuntimeConfig`, `EvalBaseline`, `EvalReport`, `load_thresholds`, `load_runtime_config`, `load_baseline`, `validate_routing_contract` |
| `archon_search/eval/live_report.py` | Builds a pass/fail/report-only verdict report from a live eval run; writes JSON + Markdown artifacts consumed by the `archon-search-eval-live.yml` CI workflow. Never raises — silently absorbs missing thresholds or baselines and falls back to `report_only`. | `MetricVerdict`, `LiveEvalReport`, `build_live_report`, `write_live_report_json`, `write_live_report_markdown`, `load_live_thresholds` |
| `archon_search/eval/metrics.py` | Recall@k, MRR, nDCG@k, reranker lift, routing accuracy, latency percentiles, chunk-to-doc dedup. | `compute_recall_at_k`, `compute_mrr`, `compute_ndcg_at_k`, `compute_reranker_lift`, `compute_routing_accuracy`, `compute_latency_percentiles`, `deduplicate_to_doc_rankings` |
| `archon_search/eval/fixtures.py` | Load `documents.jsonl`, `queries.jsonl`, `labels.jsonl`, `corpus/`. | `EvalDocument`, `EvalQuery`, `RelevanceLabel`, `EvalCorpus`, `load_eval_corpus`, `build_doc_collection_map` |
| `archon_search/eval/types.py`, `_hashing.py`, `_tracing.py` | Shared eval types, hashing helpers, trace capture. | see source |

## Test Infrastructure

`tests/integration/conftest.py` provides shared helpers used across the 19 integration/e2e test modules added by plan E1. These helpers are not production code and must not be imported from `archon_search/`. They depend on `tests/conftest.py` stubs being installed at collection time.

| Helper | Purpose | Signature |
|---|---|---|
| `make_real_app` | Build a `TestClient` backed by real `SearchStore` + `SearchPipeline` in `tmp_path`. Uses `monkeypatch.setenv` for `ARCHON_SEARCH_DATA_DIR` + `ARCHON_SEARCH_API_KEY` so env vars auto-revert after each test. Creates a real `JobScheduler` so export/import/backup dispatch works without mocking. **D5**: `maintenance_enabled=True` sets `interval_hours=1` so `POST /maintenance/trigger` fires an immediate pass during tests. | `make_real_app(tmp_path, monkeypatch, *, backup_enabled=False, maintenance_enabled=False, namespaces=None) -> tuple[TestClient, config, api_key]` |
| `ingest_doc` | POST ingest with inline text + poll `GET /jobs/{id}` until `status == 'done'` (max 10s, 100ms intervals). | `ingest_doc(client, col, text, path, *, timeout_s=10)` |
| `ingest_file_via_path` | POST ingest by filesystem path + poll until DONE. | `ingest_file_via_path(client, col, path, *, timeout_s=10)` |
| `search` | POST `/search`, assert 200, return items list. | `search(client, col, query, **filters)` |
| `make_real_pipeline` | Async helper — creates a real connected `SearchStore` + `SearchPipeline` for direct pipeline/store method calls without going through `TestClient`. Calls `await store.connect()`. | `async def make_real_pipeline(tmp_path, monkeypatch) -> tuple[SearchStore, SearchPipeline]` |

Do NOT modify `tests/conftest.py` when adding tests to `tests/integration/` — that file controls the global ML stub state shared across all workers.

## Cross-cutting

| Module | Purpose | Key public symbols |
|---|---|---|
| `archon_search/config.py` | Load/save `~/.archon-search/archon-search.toml`; coerce `telemetry.export_enabled = true` to `false` with a warning; coerce types. **C4**: adds `HyDEConfig` dataclass (`enabled`, `model`, `timeout_seconds`, `max_requests_per_minute`) and a `[hyde]` TOML section loader; `SearchConfig` gains `hyde: HyDEConfig`. **C5**: adds `RAGFusionConfig` dataclass (`enabled`, `model`, `timeout_seconds`, `max_requests_per_minute`, `num_queries`) and a `[rag_fusion]` TOML section loader; `SearchConfig` gains `rag_fusion: RAGFusionConfig`. **C9**: `load_config()` accepts `ARCHON_SEARCH_HOST` and `ARCHON_SEARCH_PORT` env vars (validated 1–65535 for port) and an `ARCHON_SEARCH_DATA_DIR` env var that overrides `db_path`, `log_file`, and `telemetry.log_dir`. Adds `serve: bool = False` kwarg that flips the host default to `0.0.0.0` for the container / direct-run path. **D6**: `SearchConfig` gains `validation_timeout_seconds: int = 60` (parsed from `[database]`; values `<= 0` rejected with a warning, fall back to `60`). **D9**: adds `McpConfig` dataclass (single field `enabled: bool = True`) and a `[mcp]` TOML section loader; `SearchConfig` gains `mcp: McpConfig`. When `enabled=false`, `create_app`'s lifespan never mounts the `/mcp` sub-app. **E0c**: `SearchConfig` gains `top_k_max: int = 100` in the `[search]` TOML section — operator ceiling on `top_k` for `POST /search` and `POST /explain`; enforced in route handler bodies (not Pydantic Field constraints). `_FANOUT_VALIDATION_LIMIT` constant removed from `routes_search.py` — both fanout and top_k upper-bound checks now read live `SearchConfig` at request time. **E0d**: adds `IngestConfig` dataclass (`max_file_mb: int = 0`); `SearchConfig` gains `ingest: IngestConfig` field. Parsed from `[ingest]` TOML section. `max_file_mb=0` (default) disables the size guard entirely; negative values raise `ConfigError` (`"[ingest].max_file_mb must be >= 0, got {raw}"`). **E1a**: adds `GraphConfig` dataclass (`enabled: bool = False`, `extraction_model: str | None = None`, `backend_threshold_edges: int`); `SearchConfig` gains `graph: GraphConfig` field. Parsed from `[graph]` TOML section. `ConfigError` raised at startup when `graph.enabled = true` and `archon-search[graph]` extras are absent. **E2a**: `MaintenanceConfig` gains `prune_expired_chunks: bool = True` — enables the expired-chunk pruning policy in `MaintenanceLoop`; parsed from `[maintenance]` TOML section. | `SearchConfig`, `TelemetryConfig`, `HyDEConfig`, `RAGFusionConfig`, `McpConfig`, `IngestConfig`, `GraphConfig`, `MaintenanceConfig`, `ConfigError`, `load_config`, `save_config`, `get_default_config_path` |
| `archon_search/paths.py` | **C9** — Single source of truth for the runtime data directory. `get_data_dir()` reads `ARCHON_SEARCH_DATA_DIR` (raises `ValueError` on empty/whitespace), falls back to `Path.home() / ".archon-search"`, and is consumed by every lazy path accessor (`key_manager.get_key_file()`, `jobs.get_jobs_file()`, `language_detector.get_fasttext_models_dir()`, `cli/ingest.py` history default, `config.load_config()` overrides). No module-level capture — the env var is read on every call so a `monkeypatch.setenv` in tests takes effect immediately. | `get_data_dir` |
| `archon_search/constants.py` | Default namespace and namespace validation. | `DEFAULT_NAMESPACE`, `_validate_namespace` |
| `archon_search/_types.py` | Internal dataclass spine: `ChunkRecord`, `SearchResult`, `DocumentInfo`, `CollectionInfo`, `IngestResult`. **C3a**: `ChunkRecord` carries two transient fields `start_offset: int = -1` and `end_offset: int = -1` (character offsets into the post-front-matter text); these are not persisted to LanceDB (absent from `_schema()` and `_do_ingest()`). **E0b**: `IngestResult` gains `warnings: list[str] = []` — populated when a non-fatal condition is encountered during ingest (e.g. ACL sidecar over 64 KB was skipped). Empty list means no warnings. **E0d**: `IngestResult` gains `code: Literal["file_too_large"] \| None = None` — `None` on success, `"file_too_large"` when the size guard rejects the file. New `IngestError` exception (`code: Literal["file_too_large"] = "file_too_large"`, `message: str`) — instantiated to produce the human-readable error message; never raised out of `pipeline.ingest_file()`. New `_file_exceeds_limit(path: Path, max_file_mb: int) -> bool` helper — shared single source of truth for boundary semantics (strictly `size > limit`; `max_file_mb=0` always returns `False`). **E2a**: `ChunkRecord` gains `expires_at: str \| None = None` and `scopes: list[str] \| None = None` (populated by pipeline from TTL/scope request params; persisted to LanceDB after `migrate_expires_at_and_scopes`). `DocumentInfo` gains `scopes: list[str] = []` (deduplicated union of scope tags across the document's chunks, sorted alphabetically). | (internal) |
| `archon_search/types.py` | Public job/collection types: `JobStatus`, `IngestJob`, `ReindexJob`, `DeleteJob`, `Query`, `RouteResponse`, `Collection`, `CollectionDetail`, `Chunk`. **E0b**: `JobStatus` gains `FAILED_EXPIRED = "FAILED_EXPIRED"` — a terminal state for `IngestJob` instances that have aged past `retry_max_age_hours` OR exhausted all retry attempts. A `FAILED_EXPIRED` job is never re-enqueued; it represents an abandoned ingest that an operator must explicitly re-trigger. Transition is one-way: `FAILED → FAILED_EXPIRED` (performed by `MaintenanceLoop`). | (public surface) |
| `archon_search/progress.py` | Indexing progress state: `IndexingStatus`, `CollectionProgress`, `IndexingState`, `IndexingStateStore`, ETA helpers. `IndexingStateStore` is thread-safe — all mutating methods (`write`, `update_collection`, `remove_collection`, `set_trigger`, `reset_in_progress`) are serialized by an internal `threading.RLock`; `read()` is an unlocked snapshot. `write()` delegates to `_durable_io.atomic_write_json` for fsync-backed atomic persistence. | `IndexingStatus`, `CollectionProgress`, `IndexingState`, `IndexingStateStore` (incl. `reset_in_progress`), `compute_eta_seconds` |
| `archon_search/key_manager.py` | Resolve the API key from env (`ARCHON_SEARCH_API_KEY`), file (`ARCHON_SEARCH_KEY_FILE`, otherwise `get_data_dir() / ".search.env"`), or generate one (durable write with mode `0600` set at creation via `_durable_io.atomic_write_bytes`). **C9**: the module-level `KEY_FILE` constant was removed in favour of `get_key_file()`, which reads `ARCHON_SEARCH_KEY_FILE` then falls back to `paths.get_data_dir()` — both reads are lazy and per-call, so `ARCHON_SEARCH_DATA_DIR` relocates the key file consistently with the rest of the runtime tree. **D7**: adds `KeyRecord` Pydantic entity model (id, token_hash, namespace, label, created_at, expires_at, status) and `KeyStore` Use Cases class: `create()`, `revoke()`, `list_keys()`, `active_keys()`, `load()`, `load_synthetic_records()`, `rotate_default_key()`. `KeyStore` persists a JSON array of `KeyRecord` objects to `get_data_dir() / "keys.json"` via `atomic_write_bytes` (mode `0o600`). Raw tokens are never stored — only SHA-256 hex digests. `active_keys()` re-reads from disk on every call. `AuthConfig` dataclass (in `config.py`) carries `rotate_grace_seconds: int = 0`. | `load_or_generate_key`, `get_key_file`, `KeyRecord`, `KeyStore` |
| `archon_search/watcher.py` | Watchdog-driven `CollectionWatcher` + `WatcherManager` with a debounced handler. | `CollectionWatcher`, `WatcherManager` |
| `archon_search/sync.py` | `SearchCollectionSync`: full reconcile between on-disk corpora and the index; manifest-based collection naming. | `SearchCollectionSync`, `SyncResult`, `path_to_collection_name` |
| `archon_search/install.py` | High-level installer: disk-space check, lock acquisition, Jina/fasttext license gates, profile-aware config write, model pre-warm, service register, health poll, rollback on failure. `NeedsForceDeleteError` signals a mismatched-profile reinstall that requires `--force --delete-db`. Key module-level helpers: `_detect_config_hand_edits` (warns before overwriting hand-edited config on re-run), `_print_next_steps` (post-install guidance), `_render_summary` (install summary with db path, server URL, API key hint, download size), `_prompt_optional_features` (7 optional-feature prompts each preceded by a plain-text explanation). Called by `cli/install_cmd.py`. | `SearchInstaller`, `NeedsForceDeleteError`, `_detect_config_hand_edits`, `_print_next_steps`, `_render_summary` |
| `archon_search/profiles.py` | Defines the three tiered install profiles (`minimal`, `balanced`, `max`) for both English and multilingual stacks. `ENGLISH_PROFILES` and `MULTILINGUAL_PROFILES` are `dict[str, InstallProfile]`; `get_profile(name, multilingual)` is the single lookup entry point. The Jina reranker constant (`JINA_RERANKER_MODEL`) lives here so the license-gate check in `install.py` has a single reference. | `InstallProfile`, `ENGLISH_PROFILES`, `MULTILINGUAL_PROFILES`, `VALID_PROFILE_NAMES`, `JINA_RERANKER_MODEL`, `get_profile` |
| `archon_search/_diagnostics.py` | Internal diagnostics helpers. | internal |
| `archon_search/_durable_io.py` | Durable fsync-backed atomic file writes (fsync file → `os.replace` → fsync parent dir). All durable JSON/bytes state writes route through here; see [130_data_architecture_and_persistence.md](130_data_architecture_and_persistence.md#durability-contract). | `atomic_write_json`, `atomic_write_bytes` |
| `archon_search/_privacy.py` | **C5** — Shared privacy utility. `_query_fingerprint(query: str) -> str` returns `sha256(query)[:16]` as a non-reversible 16-char hex log-correlation token. Used by both `hyde.py` and `rag_fusion.py` to avoid duplicate implementations. Never logs raw query text. | `_query_fingerprint` |
