# Feature Brief: Observability and Stage-Level Latency (B1 — Roadmap Item 24)

> **Order**: First item of Phase B. Ships AFTER Phase A (A1–A7). The response-surface piece (per-stage timings on `/explain`) **depends on A4** — it lands the `stage_timings_ms` field that A4 explicitly deferred to "A4.3" (`A4-explain-endpoint-brief.md:124`, `:201`). The other three deliverables — correlation IDs (`ARCH-3`), stage-timing instrumentation, and structured-log emission — are **independent of A4** and can land first. **One narrow exception:** threading `correlation_id` into the REST `/search` *telemetry* line depends on **A3**, because `routes_search.py` has no telemetry enqueue today (it returns `200`+`[]` on pipeline failure, `routes_search.py:94-96`) — A3 must create that enqueue before B1 can correlate it. The `bind_stage_recorder()` wrap, the stage-timing log record, and the `X-Request-ID` header on `/search` are all A3-independent; only the `/search` telemetry `correlation_id` is gated on A3. Resolves `ARCH-3` (`Architecture/530_technical_debt_refactoring_roadmap.md:79`).

## Problem
Today the retrieval pipeline runs five distinct stages — query embed, vector search, FTS search, RRF fusion, rerank — and the ingest path adds parse and persist, but **none of them are individually timed in production**. The only latency numbers that exist are: (a) the eval-harness p50/p95 (`tests/eval/`, report-only, deterministic stubs, explicitly *not* a production SLA per `Architecture/210_performance_and_scalability.md:13`), and (b) the per-endpoint end-to-end `latency_ms` recorded in telemetry (`routes_route.py:115`). When `/search` is slow there is no way to tell whether the embedder, the vector leg, FTS, or the cross-encoder is responsible without attaching a profiler.

Worse, **there is no request-correlation ID**. `APIKeyMiddleware.dispatch` (`middleware_auth.py:25`) sets `request.state.namespace` (and performs no other `request.state` mutation); loggers across `store.py` (`"archon"`), `routes_route.py` (`"archon.search"`), and `app.py` (`"archon-search"`) emit unstructured lines with no shared key. A single failing request cannot be traced across the auth middleware → pipeline → store → telemetry boundary post-hoc. This is `ARCH-3` in the debt register (`530_technical_debt_refactoring_roadmap.md:79`): *"Concurrent failures across `/search`, `/ingest`, and the watcher cannot be tied together post-hoc."*

## Goal
Every request carries a correlation ID minted at the edge and propagated through the pipeline into telemetry and logs. The instrumented stages — query embed, vector search, FTS search, RRF fusion, rerank on the retrieval path, and parse, embed, persist on the ingest path, plus an end-to-end `total` — are timed with a monotonic clock (`time.perf_counter()`). The per-stage breakdown is **emitted as a structured log record** for `/search`, `/route`, `/explain`, and ingest, and is **surfaced on the `/explain` response** as `stage_timings_ms` (delivering A4's deferred A4.3 field). Instrumentation is a **no-op when unbound** so library callers, the eval harness, and unit tests pay nothing and break nothing. The telemetry no-raw-query invariant is preserved — a correlation ID is a random UUID, never query-derived.

## Users & Context
- **Operators** triaging a latency regression or a production incident. They need to answer "which stage got slow?" and "show me every log line for the request that 500'd" without a profiler or a code read.
- **Maintainers** tuning the router, reranker, or hybrid scoring. Stage timings turn "the pipeline feels slow" into "rerank is 80% of the budget," and the eval harness can trend it.
- **Integrators** building on REST/MCP who hit a slow query and want a self-service breakdown via `/explain` before filing an issue.

Triggered during latency investigation, incident response, and routing/rerank tuning — the same audience and moments as A4's `/explain`, which is why B1 is co-designed with it. `archon-search` remains a single-process local service (`210_performance_and_scalability.md:9`); these are diagnostics, **not** a production SLA, and the brief does not promise one.

## Relationship to Sibling Items (intentional seams)

| Item | Overlap | B1's boundary |
|---|---|---|
| **A4** (explain endpoint) | A4 builds `/explain`, `SearchPipeline.explain()`, `_hybrid_search_with_trace` usage; A4 defers `stage_timings_ms` to "A4.3". | B1 **adds** `stage_timings_ms` to `ExplainResponse`. Purely additive over A4's schema. If A4 is unmerged, the response-surface piece is gated on it; the rest ships independently. |
| **A3** (search-failure semantics) | A3 introduces the first `logger.error(..., extra={"event_type": ...})` structured-field pattern (`A3-...-brief.md:71`) **and** the first telemetry enqueue on `/search` (none exists today — `routes_search.py:94-96` returns `200`+`[]` on failure with no `writer.enqueue`). | B1 **reuses** the same `extra={}` mechanism for a new `event_type="stage_timings"` record (same idiom, no conflict). **Hard dependency:** threading `correlation_id` into the `/search` telemetry line is gated on A3 first creating that enqueue. B1's `/search` log record + `X-Request-ID` header are A3-independent. |
| **B7** (structured logs + rotation, item 25) | B7 owns the JSON **log formatter** and **rotation policy**. | B1 emits structured log **records** via `extra={}` on the stock `logging` module; it does **not** add a JSON formatter, handler chain, or rotation. Those fields become first-class JSON keys once B7's formatter lands. Clean record-vs-format split. |
| **B2** (deeper health/readiness) | Both are "observability." | B2 is liveness/readiness probes; B1 is per-request tracing + stage latency. Disjoint. |

## Core Flow
1. A request arrives. A new **pure-ASGI** `RequestContextMiddleware` (added **outermost** — see Key Decisions for why it must not be `BaseHTTPMiddleware`) reads an inbound `X-Request-ID` header if present and valid, otherwise mints `uuid4().hex`. It sets the `correlation_id` ContextVar, stores `request.state.request_id`, and echoes `X-Request-ID` on the response. This runs for **every** path including `/health` and `401`s.
2. The `/search`, `/route`, or `/explain` handler enters a `bind_stage_recorder()` context that installs a fresh `StageRecorder` in a ContextVar for the duration of the request. (MCP tool handlers in `mcp.py` do the same, binding inside the tool body; they read `correlation_id` from the ContextVar set by the MCP app's own `RequestContextMiddleware` registration — see In Scope and Edge Cases — since MCP tools have no `request.state`.)
3. As the pipeline runs, each stage wraps its work in `record_stage("<name>")`:
   - `record_stage("embed")` around `Embedder.embed_one` (`pipeline.py:320`),
   - `record_stage("route")` around centroid scoring in A4's shared `MultiCollectionRouter._score_collections` helper — which A4 extracts from the `router.py:141-146` centroid loop and shares between `rank()` and `rank_with_scores()` — so the `route` stage is timed on collectionless `/explain` (via `rank_with_scores()`) and `/route` (via `rank()`). It is **not** timed on `/search`, which takes a pinned `collection` and calls `pipeline.search(...)` directly without ever invoking the router. On `/route` the centroid `rank()` only runs in Tier 3 (`router.py:203-205`, when `n_routable > shortlist_size`); Tiers 1-2 skip centroid ranking, so the `route` stage is absent for Tier 1-2 routing,
   - `record_stage("vector")`, `record_stage("fts")`, `record_stage("fuse")` inside `store.hybrid_search` (`store.py:655`, `:662`, `:677-687`) **and** the trace twin `_hybrid_search_with_trace` (`store.py:946-948`, `:962-969`, `:1000-1004`),
   - `record_stage("rerank")` around `Reranker.rerank` (`reranker.py:56`),
   - `record_stage("context")` around the neighbor-fetch loop in `SearchPipeline.search_with_context` (`pipeline.py:340-343`) — it calls `self.search()` then loops `store.fetch_adjacent_chunks` + a second `apply_acl_filter` per result, a stage that can dominate latency. So `search_with_context` carries the key set `{embed, vector, fts, fuse, rerank, context, total}` (the `context` stage is added on top of the search stages it wraps). `route` is absent because `search_with_context` runs against a pinned collection in `DEFAULT_NAMESPACE`; it would appear only if `search_with_context` were ever called collectionless (as on collectionless `/explain`).
   `record_stage` is a **no-op when no recorder is bound** (ContextVar is `None`) — so direct `SearchPipeline.search()` callers and the eval backends record nothing and incur a single `ContextVar.get()` per stage.
4. After the pipeline returns, the handler records `total` as a `time.perf_counter()` delta (the same clock as every sub-stage, so all values in `stage_timings_ms` share one clock; the separate telemetry `latency_ms` field keeps its own `monotonic()` timer at `routes_route.py:71`), then emits one structured log record: `logger.info("stage timings", extra={"event_type": "stage_timings", "correlation_id": ..., "endpoint": ..., "collection": ..., "stage_timings_ms": {...}})`.
5. The telemetry enqueue (`routes_route.py:111`, `mcp.py`) passes the `correlation_id` into the `TelemetryEntry.from_*` factory via a new optional kwarg, so the JSONL line and the log records share a join key. **Stage timings are not written to telemetry** — only the correlation ID (see Key Decisions).
6. For `/explain` only, the route maps the recorder's collected timings into the new `ExplainResponse.stage_timings_ms` field. `/search` and `/route` do **not** gain a response field — their timings live only in the structured log.
7. The ingest path wraps `parse`/`embed`/`persist` **only inside `SearchPipeline.ingest_file` (`pipeline.py:150` parse, `:195` embed, `:202-204` persist)** — the leaf convergence point every real caller ultimately hits — and emits a `stage_timings` structured-log record with key set `{parse, embed, persist, total}` and `endpoint="ingest"`. `ingest_directory` (`pipeline.py:211`) has no parse/embed/persist of its own to wrap: it collects/filters files and reaches that same instrumentation through its per-file `ingest_file` loop (`pipeline.py:256`), so a directory ingest records each stage once per file into the StageRecorder's per-stage lists. Do **NOT** add a second set of `record_stage` calls in `ingest_directory`'s body — that would double-record every per-file stage. The no-op-when-unbound guard keeps `ingest_file` free for unbound callers (eval/tests). The entry points that bind a recorder and emit the log are the **MCP `ingest_file`/`ingest_directory` tools** (`mcp.py:134`, `:149` — the real REST/MCP callers of `pipeline.ingest_*`) and the **CLI** (whose ingest paths, `cli/ingest.py` and `cli/collection.py`, call `ingest_directory` not `ingest_file`, so the CLI's emitted record is per-directory and aggregated); the CLI ingest path **mints its own correlation ID** (there is no inbound `X-Request-ID`). The detached job execution lives in `server/routes_jobs.py` (`_default_ingest_task`/`_run_pipeline`), driving the injected `app.state.ingest_pipeline` callable — **not** `jobs/`, which is persistence only (`JobStore` + model, no execution code). **Production reality:** `app.state.ingest_pipeline` is never set anywhere in `archon_search/` (not in `create_app`), referenced only at `routes_jobs.py:95-97`, so today the HTTP `/ingest` job path runs the stub branch (`await asyncio.sleep(0)`, `routes_jobs.py:51-55`) and emits **no** ingest stage-timing record until that pipeline callable is wired. Once `ingest_pipeline` is wired, the job runner binds a recorder and the per-job `correlation_id` **reuses the existing job ID** (no minted UUID, no inbound header). To avoid log spam on bulk directory ingests, this is logged **per-file at DEBUG**, or **aggregated per-job at INFO** when an ingest job processes many files — the entry-point log record for a directory ingest aggregates those per-file lists. Ingest has no response surface — timings are log-only.

## Public Contract

### Correlation ID
- Response header `X-Request-ID` on every response (including `/health`, `401`, `5xx`).
- Inbound `X-Request-ID` is honored when it matches `^[A-Za-z0-9._-]{1,128}$`; otherwise it is **discarded and regenerated** (log-injection / forgery guard — see Edge Cases).
- New additive field on `TelemetryEntry` (`telemetry/entry.py:57` class; fields from `:60`): `correlation_id: str | None = None`, added to `DOCUMENTED_SCHEMA_FIELDS` (`entry.py:39-54`). All four factories (`from_search_tool_result`, `from_route_response`, `from_error`, plus A4's `from_explain_result` when A4 is merged) gain an optional `correlation_id` keyword. **No `query` parameter is added** — the structural no-raw-query invariant is untouched.

### `stage_timings_ms` on `/explain` (delivers A4.3)
Additive field on `ExplainResponse` (the A4 schema in `routes_explain.py`):

```json
{
  "rerank": true,
  "routing": { "...": "as A4" },
  "collection": "docs",
  "acl_filtered": false,
  "results": [ { "...": "as A4" } ],
  "near_misses": [ { "...": "as A4" } ],
  "stage_timings_ms": {
    "embed": 4.1,
    "route": 1.8,
    "vector": 6.2,
    "fts": 3.0,
    "fuse": 0.4,
    "rerank": 88.5,
    "total": 104.7
  }
}
```

- `stage_timings_ms: dict[str, float]`. Keys are present only for stages that **actually ran**: `route` appears only when `collection` was omitted; `rerank` is absent when `rerank=false`; `fts` is omitted when no FTS index exists. The omission is **mechanical**: `hybrid_search` (`store.py:662-670`) and `_hybrid_search_with_trace` (`store.py:962-978`) catch the missing-index error **internally** — no exception escapes, `fts_rows` stays `[]` — so the `fts` stage timing is recorded **only on the successful-FTS branch** (inside the `try`, after `fts_rows`/`fts_rank` are populated). A caught degradation therefore leaves no `fts` entry; the `record_stage("fts")` block is never entered on that path. (This is distinct from a stage that *actually raises* — see the Edge Cases row, where `record_stage` records elapsed in `finally` and re-raises.) `total` is always present.
- Values are elapsed milliseconds: every value, sub-stages **and** `total`, is a `time.perf_counter()` delta so the whole dict shares one clock. (Telemetry's separate `latency_ms` field keeps its own `monotonic()` timer at `routes_route.py:71` — unchanged.) All **non-deterministic** — tests assert on key *presence*, never on numeric values (see Acceptance Criteria #5).
- MCP `explain` returns the identical structure (A4's REST↔MCP parity extends to this field).
- Field is **omitted entirely** (not `null`) when stage timing is disabled via config (see below), keeping the A4 baseline response byte-identical for operators who turn it off.

### Structured log record
One record per instrumented request, emitted via the stock `logging` module (logger `"archon.search"`):
```python
logger.info("stage timings", extra={
    "event_type": "stage_timings",
    "correlation_id": "<uuid4hex>",
    "endpoint": "search" | "route" | "explain" | "ingest",
    "collection": "<name or None>",
    "stage_timings_ms": {"embed": ..., "vector": ..., ..., "total": ...},
})
```
On the ingest path the same record shape is emitted with `endpoint="ingest"` and `stage_timings_ms` carrying the ingest key set `{parse, embed, persist, total}` (logged per-file at DEBUG or aggregated per-job at INFO). The MCP `search_with_context` tool emits the record with the key set `{embed, vector, fts, fuse, rerank, context, total}` (the search stages plus the neighbor-fetch `context` stage); `route` is absent because it runs against a pinned collection in `DEFAULT_NAMESPACE`, and would appear only if it were ever called collectionless (as on collectionless `/explain`). Asserted via captured `LogRecord` attributes (`record.event_type`, `record.correlation_id`), **not** message substring — same mechanism A3 prescribes (`A3-...-brief.md:71`). Rendering these fields as JSON is **B7's** job, not B1's.

## In Scope
- New module `archon_search/observability.py` containing:
  - `correlation_id: ContextVar[str | None]` and `new_correlation_id() -> str`.
  - `StageRecorder` — accumulates stage timings using `time.perf_counter()`, storing per-stage values internally as `dict[str, list[float]]` (or last-write-wins with a logged warning on collision) **rather than silently summing** repeated calls. The public `stage_timings_ms: dict[str, float]` stays single-valued for v1 because no v1 path runs a stage twice. Storing lists now is forward-compatible: B3 (server-side multi-collection search) embeds once and fans retrieval out across collections, where silent summing would hide per-collection attribution (see Future Iterations).
  - `_stage_recorder: ContextVar[StageRecorder | None]` and `@contextmanager record_stage(name)` that is a **no-op when the ContextVar is unset** (single `.get()` + early return).
  - `@contextmanager bind_stage_recorder()` that installs a fresh recorder and resets the ContextVar in `finally` via **Token-based reset** (`token = var.set(...); try: ... finally: var.reset(token)`), **never** `var.set(None)` — so nested binds (a test that binds, then calls a handler that binds again) restore the outer context instead of clobbering it. The `correlation_id` setter in the middleware uses the same Token-based reset.
  - `sanitize_request_id(raw: str | None) -> str | None` enforcing the charset/length rule.
- New **pure-ASGI** `RequestContextMiddleware` in `archon_search/server/middleware_context.py` (implements `async def __call__(self, scope, receive, send)`, **not** `BaseHTTPMiddleware`). Mints/propagates the correlation ID, sets the ContextVar, sets `request.state.request_id`, and injects the `X-Request-ID` response header by wrapping `send` (see Key Decisions for the wrapper mechanism). Registered in `create_app` **after** the existing `add_middleware` calls (`app.py:121-122`) so it is outermost (LIFO order).
- **MCP middleware registration.** Register the **same** `RequestContextMiddleware` on the separate MCP Starlette app inside `create_mcp_http_app` (`mcp.py:247-262`) — it composes with `streamable_http_app()` (`mcp.py:259`) alongside the existing `APIKeyMiddleware` (`mcp.py:261`). The MCP app is NOT mounted into the FastAPI app, so it does not inherit `create_app`'s middleware; this second registration is what gives MCP correlation-ID + header parity. MCP tools read `correlation_id` from the ContextVar (they have no `request.state`) and wrap their body in `bind_stage_recorder()`; MCP runs in `DEFAULT_NAMESPACE` only (`namespaces={}`).
- Stage instrumentation (additive `record_stage(...)` wrappers only — **no signature changes**) in: `Embedder.embed`/`embed_one` (`embedder.py:59-68`), A4's shared `MultiCollectionRouter._score_collections` helper (extracted from the `router.py:141-146` centroid loop and shared between `rank()` and `rank_with_scores()`, so the `route` stage is timed on collectionless `/explain` and `/route` — but **not** `/search`, which calls `pipeline.search(...)` against a pinned collection without invoking the router; and on `/route` the centroid `rank()` runs only in Tier 3 (`router.py:203-205`), so the `route` stage is absent for Tier 1-2 routing), `SearchStore.hybrid_search` (`store.py:655`, `:662`, `:677-687`), `_hybrid_search_with_trace` (`store.py:946-948`, `:962-969`, `:1000-1004`), `Reranker.rerank` (`reranker.py:56`), the `SearchPipeline.search_with_context` neighbor-fetch loop (`pipeline.py:340-343`, the `context` stage), and the ingest path **only inside `SearchPipeline.ingest_file`** (`pipeline.py:150` parse, `:195` embed, `:202-204` persist) — the leaf convergence point all real ingest callers ultimately hit; `ingest_directory` (`pipeline.py:211`) wraps nothing of its own, reaching this instrumentation through its per-file `ingest_file` loop (`pipeline.py:256`), so do **not** add a second set of `record_stage` calls there.
- `bind_stage_recorder()` + post-call structured-log emission in REST handlers `/search` (`routes_search.py`), `/route` (`routes_route.py`), `/explain` (`routes_explain.py`, from A4), and the MCP tools in `mcp.py`: the retrieval tools `search`, `search_with_context`, and A4's `explain`, **plus the ingest tools `ingest_file` (`mcp.py:134`, calls `pipeline.ingest_file` at `:140`) and `ingest_directory` (`mcp.py:149`, calls `pipeline.ingest_directory` at `:161`)** — the real REST/MCP callers of `pipeline.ingest_*` today.
- **Ingest binding seam.** Instrument `parse`/`embed`/`persist` **only inside `SearchPipeline.ingest_file`** (the leaf convergence point all real callers ultimately hit); the no-op-when-unbound guard keeps it free for unbound callers. `ingest_directory` (`pipeline.py:211`) has no parse/embed/persist of its own — it collects/filters files and loops over `ingest_file` (`pipeline.py:256`), so it reaches this instrumentation through that loop and records each stage once per file into the StageRecorder's per-stage lists; **do not add a second set of `record_stage` calls in `ingest_directory`** (that would double-record). The entry points that bind a recorder and emit a `stage_timings` record (`endpoint="ingest"`, key set `{parse, embed, persist, total}`, per-file at DEBUG or aggregated per-job at INFO to avoid log spam) are the **MCP `ingest_file`/`ingest_directory` tools** (`mcp.py:134`, `:149`) and the **CLI** today, plus the job runner once `app.state.ingest_pipeline` is wired. The CLI ingest paths (`cli/ingest.py`, `cli/collection.py`) call `ingest_directory` (not `ingest_file`), bind a recorder locally and **mint their own correlation ID** (no inbound header), so the CLI's emitted record is per-directory (aggregated) and reaches the per-file instrumentation through the loop. The detached job execution lives in `server/routes_jobs.py` (`_default_ingest_task`/`_run_pipeline`, spawned via `asyncio.create_task`), driving the injected `app.state.ingest_pipeline` callable — **not** `jobs/`, which is persistence only. **In production today `app.state.ingest_pipeline` is unset** (referenced only at `routes_jobs.py:95-97`, never assigned in `archon_search/`), so the HTTP `/ingest` job path runs the stub (`await asyncio.sleep(0)`, `routes_jobs.py:51-55`) and emits no ingest stage-timing record until the pipeline callable is wired; when it is, the job runner binds a recorder whose per-job `correlation_id` **reuses the existing job ID**.
- `correlation_id` field on `TelemetryEntry` + `DOCUMENTED_SCHEMA_FIELDS` + the four factories (`from_search_tool_result`, `from_route_response`, `from_error`, plus A4's `from_explain_result` when A4 is merged); threaded from the ContextVar at every existing enqueue site (`routes_route.py:111-161`, `mcp.py`, plus A3's `/search` error enqueue and A4's `/explain` enqueue).
- `stage_timings_ms` field on `ExplainResponse` + population in the `/explain` route (depends on A4).
- New `[observability]` config section parsed in `config.py` (mirroring the `[telemetry]` block at `config.py:200-223`): `stage_timings_enabled: bool = True`, `request_id_header: str = "X-Request-ID"`. Surfaced on the `SearchConfig` dataclass (`config.py:27-55`) and `archon-search config`.
- OpenAPI: `stage_timings_ms` documented on the `/explain` response model.
- Docs: `Architecture/160_operational_readiness_monitoring_and_reliability.md` (correlation-ID + stage-timing surface), `Architecture/210_performance_and_scalability.md` (new in-process measurement surface, still report-only — not an SLA; document that stage timings are blocked-coroutine wall time including executor-queue/event-loop latency, not pure stage CPU), and `530_technical_debt_refactoring_roadmap.md:79` (mark `ARCH-3` resolved).

## Out of Scope
- **JSON log formatter, handler wiring, and log rotation** — owned by **B7** (item 25). B1 emits `extra={}` records on the stock logger; it does not touch `config.log_file` (`config.py:51`) handler setup. The `log_file` path is configured but unwired today (verified — no `dictConfig`/`basicConfig` in the tree); B1 does not fix that.
- **Stage timings inside the telemetry JSONL.** Only `correlation_id` is added. Telemetry stays minimal (`entry.py`'s deliberately small surface); per-stage breakdown lives in logs and on `/explain`. Avoids inflating `MAX_ENTRY_BYTES` budgets (`writer.py:33`).
- **`stage_timings_ms` on `/search` or `/route` responses.** Keeps the hot endpoints' response shape unchanged (same lean-response philosophy A4 applies to `/search`). Their timings are log-only.
- **OpenTelemetry / OTLP spans / distributed tracing exporters** — future (B1.1). B1 is in-process timing + a correlation ID, not a tracing backend.
- **A `/metrics` Prometheus endpoint or histograms** — future; B1 records per-request point values, not aggregates.
- **Eval-harness latency *gating* on per-stage numbers.** Latency ceilings stay report-only (`210_performance_and_scalability.md:23`, `tests/eval/thresholds.toml`). B1 does not add a per-stage threshold gate.
- **A separate `acl` stage timing.** The ACL-filter step (a cheap in-memory list filter between fuse and rerank, `pipeline.py:322`) is **not** separately timed in v1; its cost folds into the surrounding stages. Additive later if it ever proves material.
- **Timing the watcher/sync background paths** (`watcher.py`, `sync.py`) — request-path and ingest-job instrumentation only in v1.
- **Sampling / rate-limiting of timing logs** — single record per request; high-QPS sampling deferred (single-process local service, low volume).
- **Renaming the inconsistent logger names** (`"archon"` / `"archon.search"` / `"archon-search"`) — orthogonal cleanup; B1 adds fields, not a logging-namespace refactor.

## Key Decisions
- **Pure-ASGI middleware, not `BaseHTTPMiddleware`.** The existing `APIKeyMiddleware` is a `BaseHTTPMiddleware` (`middleware_auth.py:19`) and works only because it mutates `request.state` (which is the request object, shared across the call). `BaseHTTPMiddleware` runs the downstream app in a **separate anyio task**. On the pinned Starlette, *forward* propagation does in fact work — anyio copies the current context at task spawn, so a `ContextVar` set inside `dispatch` **before** `call_next` is visible in the endpoint. The real hazards are (a) the **reverse** direction — a `ContextVar` set by the endpoint/downstream is **not** visible back in `dispatch` after `call_next` returns (it ran in a child context); (b) this behavior is **version-sensitive** across Starlette/anyio releases; and (c) `BaseHTTPMiddleware` is widely documented as unreliable for `ContextVar` propagation in general. Rather than depend on a fragile forward-copy guarantee, `RequestContextMiddleware` MUST be a pure-ASGI middleware (set the ContextVar in `__call__` before awaiting the inner app, in the **same task/context** that runs the endpoint, so the var is unambiguously visible downstream). This is the single most important correctness constraint in the brief.
- **`X-Request-ID` injection via a `send` wrapper.** The middleware wraps the ASGI `send` callable: it intercepts only `message["type"] == "http.response.start"`, appends `(b"x-request-id", value.encode())` to `message["headers"]` (de-duping if the inner app already emitted the header), and passes **all other** ASGI messages (`http.response.body`, etc.) through untouched. Because `app.py` uses LIFO `add_middleware` (current order `APIKeyMiddleware` then `CORSMiddleware`, `app.py:121-122`), adding `RequestContextMiddleware` **last** makes it **outermost** — so it also wraps CORS-preflight `OPTIONS` requests that `CORSMiddleware` short-circuits and auth-rejected `401`s. This is **intended**: you want a correlation ID on CORS/auth-rejected requests for tracing too (see Acceptance Criteria for the `OPTIONS`-preflight check).
- **ContextVar propagation, not signature threading.** Adding a `correlation_id`/`recorder` parameter to `search`, `hybrid_search`, `rerank`, `rank`, `embed`, … would touch a dozen signatures and ripple into every caller and test. A ContextVar bound at the route layer reaches the awaited pipeline because they share the request task's context. The embed/rerank legs await `asyncio.to_thread(...)` (`embedder.py`, `reranker.py:56`) — but B1 times *around* the `await` in the event loop, never inside the worker thread, so thread-context copying is irrelevant.
- **No-op-when-unbound is non-negotiable.** `record_stage` checks the ContextVar and returns immediately when unset. This guarantees: (a) the eval harness and its deterministic backends record nothing and stay byte-stable, (b) unit tests calling `SearchPipeline.search()` directly need no fixture, (c) library/import users pay one `ContextVar.get()` per stage. The instrumentation is invisible unless a server (or a test) explicitly binds a recorder.
- **`perf_counter` for all `stage_timings_ms` values, including `total`.** `time.perf_counter()` is the highest-resolution monotonic timer and is the correct primitive for elapsed-time measurement; **every** value in `stage_timings_ms` — sub-stages *and* `total` — uses `time.perf_counter()` so they share one clock. Telemetry's existing per-endpoint `latency_ms` field stays on `monotonic()` (`routes_route.py:71`) — that is a separate, unchanged field, not part of `stage_timings_ms`.
- **Stage timings measure blocked-coroutine wall time, not pure stage CPU.** The `embed` and `rerank` stages await `asyncio.to_thread(...)` on the shared default executor, so under concurrency a stage's measured time **includes** time queued behind other `to_thread` work plus event-loop scheduling latency — it is the wall-elapsed time the awaiting coroutine was blocked on that stage, NOT that stage's pure CPU. This is acceptable for the report-only diagnostic framing; true per-stage CPU spans are deferred to **B1.1** (OpenTelemetry).
- **Correlation ID added to telemetry; raw query never is.** A **minted** `correlation_id` is a server-generated random token (`uuid4().hex`) with no user content. An **honored inbound** `X-Request-ID` is client-controlled, but is constrained to `^[A-Za-z0-9._-]{1,128}$` by `sanitize_request_id` — so it cannot carry query text or control characters, which is what the structural no-raw-query invariant guards against. Either way the value is safe for the local-JSONL/log threat model. The factories keep rejecting a `query` kwarg (`extra="forbid"`, `frozen=True` on the model, `entry.py:58`). This is verified by a structural test (Acceptance Criteria #8).
- **Timings on `/explain` only, logs everywhere.** `/explain` is the sanctioned debug surface (A4); a response field there is expected. `/search`/`/route` stay lean — operators read their timings from the structured log keyed by the same correlation ID. This mirrors A4's refusal to add `?explain=true` polymorphism to `/search`.
- **B1 owns records, B7 owns format.** Emitting `extra={}` fields is additive and harmless without a JSON formatter (they're just ignored by the default formatter). This lets B1 ship before B7 and lets B7 light them up later with zero B1 rework.
- **Config default-on, cheaply disableable.** `stage_timings_enabled` defaults to `True` because overhead is sub-microsecond per stage, but operators on a hot single-core host can disable it; when off, `bind_stage_recorder()` installs nothing, `record_stage` no-ops, and `/explain` omits the field.

## Edge Cases & Constraints

| Scenario | Behavior |
|---|---|
| No recorder bound (library call, eval backend, unit test) | `record_stage` no-ops; zero timings collected; no error. |
| `rerank=false` on `/explain` | `rerank` key absent from `stage_timings_ms`; results sorted by `rrf_score` (A4 contract unchanged). |
| Collection pinned on `/explain` | `route` key absent (router never ran), consistent with A4's `routing: null`. |
| No FTS index (`store.py:662-670` / `:962-978` degradation) | The missing-index error is caught **internally** (no exception escapes, `fts_rows` stays `[]`), so the `record_stage("fts")` block on the successful-FTS branch is never entered → `fts` key omitted; `vector`, `fuse`, `total` still present; no crash. Distinct from a stage that actually raises (the "Stage raises mid-timing" row below). |
| Inbound `X-Request-ID` with newlines / control chars / >128 chars | Rejected by `sanitize_request_id`; a fresh UUID is minted. **Prevents log-injection and cross-request ID forgery.** |
| Inbound `X-Request-ID` valid | Honored verbatim (supports tracing across an upstream gateway). |
| Stage raises mid-timing (e.g. embedder throws) | The `record_stage` context manager records elapsed time in `finally` and re-raises; the partial timing is still logged on the error path (pairs with A3's error telemetry). |
| `stage_timings_enabled=false` | No recorder bound; `/explain` omits `stage_timings_ms`; structured-log record not emitted; correlation ID still minted and still added to telemetry. |
| Telemetry disabled (`telemetry_writer is None`, `app.py:105`) | Correlation ID still on logs + response header; no telemetry write (existing guard). |
| MCP request | MCP runs as a **separate** Starlette app (`create_mcp_http_app`, `mcp.py:247-262`) — it is NOT mounted into the FastAPI app and does NOT share its middleware stack. B1 registers the same pure-ASGI `RequestContextMiddleware` on that MCP app too (it composes with `streamable_http_app()`, `mcp.py:259`). MCP tool handlers (e.g. `search`, `mcp.py:48`; the ingest tools `ingest_file`/`ingest_directory`, `mcp.py:134`/`:149`) take no `Request` and have no `request.state`, so they read `correlation_id` from the ContextVar set by that middleware and wrap their body in `bind_stage_recorder()`. MCP runs in `DEFAULT_NAMESPACE` only (`namespaces={}`, `mcp.py:261`). |
| Concurrent requests | Each runs in its own task with its own ContextVar context; recorders and IDs do not leak across requests. |

**Privacy / leakage:**
- A **minted** `correlation_id` is a random UUID — no path, no query, no principal. An **honored inbound** `X-Request-ID` is client-controlled but constrained to `^[A-Za-z0-9._-]{1,128}$` by `sanitize_request_id`, so it cannot carry query text or control characters. Both are safe in logs, telemetry, and the response header.
- Stage timings are pure floats — no content. `source_path` already appears in `/explain` results per A4; B1 adds no new content exposure.
- The no-raw-query invariant is preserved structurally; enforced by test (#8).

## Open Questions
- **Does FastMCP's streamable-HTTP transport fork a task between the pure-ASGI middleware and the tool coroutine?** B1 registers `RequestContextMiddleware` directly on the MCP Starlette app (`create_mcp_http_app`), so the ContextVar is set before the inner app runs — but verify during planning that the streamable-HTTP transport does not spawn a child task between the middleware and the tool body (the same hazard as `BaseHTTPMiddleware`). If it does, the fallback is to have the tool body **read** the id the middleware already set (from the request scope / a transport-provided context object) and mint a fresh one **only if none is found** — **never** mint in a way that diverges from the `X-Request-ID` the middleware's `send`-wrapper already emitted on the response. The MCP response header and the MCP log/telemetry `correlation_id` **MUST match** (a divergent id defeats the header↔log join); asserted by Acceptance Criteria #18.
- **Field name: `correlation_id` vs `request_id`?** Default to `correlation_id` on the telemetry entry (distinct from the existing per-entry `query_id`, `entry.py:60`) and `X-Request-ID` on the wire header (industry-standard header name). Confirm no operator-facing naming clash during planning.
- **Should `/explain` also echo `correlation_id` in its JSON body?** Default **no** — operators read it from the `X-Request-ID` response header, matching A4's choice not to echo the query. Revisit only if integrators ask.
- **Per-leg timing inside `hybrid_search` on the hot path** — confirm the bound `record_stage` overhead is acceptable (three extra `perf_counter` pairs per search). Expected negligible vs. model inference; the bound path is exercised by the success-path timing tests (Acceptance Criteria #5/#6), but no hard latency bound is asserted — B1 is report-only.

## Acceptance Criteria

Concrete assertions a planner can convert to tests. Marker in brackets (`default` = unrestricted, `eval` = marker-gated).

1. **Correlation ID on every response** [default] — `X-Request-ID` header present on `200`, on `401` (bad/no token), and on `/health` (exempt path). Header value matches the sanitize charset.
2. **ID propagates to log + telemetry** [default] — a single `/route` call with telemetry enabled (writer → tmp dir); call `await writer.drain_and_stop()` (`app.py:114`) to flush the async writer **before** reading the JSONL, then assert the JSONL line's `correlation_id` equals the `stage_timings` log record's `correlation_id` equals the response `X-Request-ID`. (The explicit drain avoids racing the async telemetry writer.)
3. **Inbound ID honored / sanitized** [default] — valid inbound `X-Request-ID` is echoed back unchanged; an inbound value containing `\n` or exceeding 128 chars is replaced by a fresh UUID (assert the response header differs from the malicious input and matches the charset).
4. **No-op when unbound** [default] — call `SearchPipeline.search(...)` directly (no server, no `bind_stage_recorder`); assert it returns normally and that `_stage_recorder.get()` is `None` throughout (no timings recorded, no exception). Note overhead: one `ContextVar.get()` per stage.
5. **`stage_timings_ms` keys on `/explain`** [default] — against A4's pre-built fixture (`tests/fixtures/explain_corpus/`): pinned collection + `rerank=true` → keys `== {embed, vector, fts, fuse, rerank, total}`; collectionless → adds `route`; `rerank=false` → `rerank` absent. **Assert keys only, never values** (timings are non-deterministic). Each value is a non-negative float.
6. **Structured log record shape** [default] — on the **success path**, `/search`, `/route`, `/explain` each emit exactly one record **with `event_type == "stage_timings"`** (filtered from `caplog.records` — other loggers emit too, e.g. the FTS-absent warning and the auth `logger.debug`), carrying a `record.correlation_id` and a `record.stage_timings_ms` dict with a `total` key. On a `5xx` the error log is a separate record, so the "exactly one `stage_timings` record" assertion is scoped to the success path. Asserted via captured-record attributes, not message text.
7. **Ingest stage-timing record** [default] — bind a recorder (`bind_stage_recorder()`) and call `SearchPipeline.ingest_file` directly on a single file (the way a unit test exercises the convergence point); assert it emits a `LogRecord` with `record.event_type == "stage_timings"`, `record.endpoint == "ingest"`, and a `record.stage_timings_ms` dict whose key set `== {parse, embed, persist, total}`. **Assert keys only, never values.** Asserted via captured-record attributes, not message text. (This exercises the instrumented pipeline method directly; it does **not** drive the HTTP `/ingest` job runner, whose `app.state.ingest_pipeline` callable is unwired in production today.)
8. **Telemetry correlation_id (positive + structural)** [default] — (a) the JSONL line for a `/route` call contains `correlation_id` and not `query`; (b) structural: `"correlation_id" in inspect.signature(TelemetryEntry.from_route_response).parameters` and `"query" not in` it; (c) constructing `TelemetryEntry(query="x", ...)` still raises (model `extra="forbid"`).
9. **FTS-absent degradation** [default] — corpus with no FTS index → `/explain` `stage_timings_ms` omits `fts`, includes `vector`/`fuse`/`total`; no error; `fts_score: null` still honored per A4.
10. **REST↔MCP key parity** [default] — for identical inputs, `set(rest.stage_timings_ms) == set(mcp_explain.stage_timings_ms)` (values differ, keys match).
11. **Disabled path** [default] — with `[observability].stage_timings_enabled=false`: `/explain` response has no `stage_timings_ms` key (A4 baseline response byte-shape restored); no `stage_timings` log record; `X-Request-ID` header and telemetry `correlation_id` still present.
12. **Concurrency isolation** [default] — fire two concurrent `/search` requests with distinct inbound `X-Request-ID`s; assert each response echoes its own ID and neither recorder's timings leak into the other (distinct correlation IDs in the two log records).
13. **Eval stability** [eval] — running `uv run pytest -m eval` with B1 merged produces the same `routing_accuracy` / retrieval metrics as baseline; instrumentation does not perturb ranking. (The eval backends bind no recorder, so this is a regression guard on the no-op path.)
14. **Coverage** [default] — default `--cov-fail-under=85` stays green without amendment. Focused unit tests on `StageRecorder`, `sanitize_request_id`, and the middleware cover the new branches.
15. **`OPTIONS` preflight carries the ID** [default] — a CORS-preflight `OPTIONS` request (short-circuited by `CORSMiddleware`) still receives an `X-Request-ID` response header matching the sanitize charset — confirming `RequestContextMiddleware` is outermost and wraps requests `CORSMiddleware` short-circuits.
16. **`search_with_context` context stage** [default] — calling `SearchPipeline.search_with_context` (or the MCP `search_with_context` tool) with a bound recorder emits a `stage_timings` record whose key set `== {embed, vector, fts, fuse, rerank, context, total}` (the `context` stage wraps the neighbor-fetch loop; `route` is absent because the call runs against a pinned collection in `DEFAULT_NAMESPACE`). **Assert keys only, never values.**
17. **Nested-bind isolation** [default] — a test that calls `bind_stage_recorder()` and, inside it, invokes a handler that calls `bind_stage_recorder()` again, asserts that on exit of the inner bind the outer recorder is restored (Token-based `var.reset(token)`, not `var.set(None)`), and the outer recorder is unchanged by the inner one.
18. **MCP header↔log correlation_id equality** [default] — for an MCP tool call (e.g. `search`), assert the `correlation_id` on the emitted `stage_timings` log record (and on the telemetry line) **equals** the `X-Request-ID` the MCP middleware emitted on the response — guarding against the task-fork fallback minting a divergent id in the tool body.

### Test fixtures
- **`/explain` timing tests (#5, #9, #10)** reuse A4's committed `tests/fixtures/explain_corpus/` (5–6 docs, ~30 chunks) and deterministic backends — determinism applies to *keys and ordering*, not to timing values.
- **Telemetry / log tests (#2, #6, #7, #8)** follow the `tests/server/test_routes_route_telemetry.py` pattern (minimal app with `app.state.telemetry_writer = writer_mock`) that A3 also builds on.
- **Middleware tests (#1, #3, #12)** use a bare `create_app()` instance; ContextVar isolation is asserted with `asyncio.gather` of two requests.

## Future Iterations
- **B1.1**: OpenTelemetry / OTLP span export — turn the `StageRecorder` into spans on a configured exporter. Additive.
- **B3 per-collection stage attribution**: server-side multi-collection search embeds once and fans retrieval across collections, so a single request runs the retrieval stages once *per collection*. The `StageRecorder` already stores per-stage values as lists (see In Scope) precisely so B3 can surface per-collection attribution instead of a silently summed scalar; the v1 single-valued public surface widens then, not now.
- **B1.2**: `/metrics` (Prometheus) with per-stage latency histograms. Additive.
- **B7** (sibling): JSON log formatter + rotation lights up the `extra={}` fields B1 emits.
- **A4.1 / A4.2** (from A4): `matched_filters` / `expansions` fields land independently; `stage_timings_ms` composes with them on `ExplainResponse`.
- **CLI**: `archon-search explain "query" --timings` pretty-print, and a `--trace` flag on `archon-search search`. Defer until requested.
- **Sampling**: probabilistic timing-log emission if a high-QPS deployment ever makes per-request logging noisy.

## Recommendation
Ship this as Phase B's opener, **after** A1–A7 and **co-sequenced with A4** — the correlation-ID, instrumentation, and structured-log pieces are independent and can land first; the `stage_timings_ms` response field is a thin additive layer on A4's `/explain` once A4 merges. The work is bounded (~220–320 LOC of production code — one new module, one pure-ASGI middleware registered on **both** the FastAPI app and the separate MCP Starlette app with per-tool binding, the ingest seam inside `SearchPipeline.ingest_file` only — `ingest_directory` reaches it through its per-file `ingest_file` loop, so no second wrapper there — bound by the MCP ingest tools and the CLI today, plus the `routes_jobs.py` runner once `app.state.ingest_pipeline` is wired), ~12-14 `record_stage` call sites — ~8 logical stages, with the store legs instrumented in both the plain and trace paths — one telemetry field, one schema field — plus ~350–500 LOC of tests; ~180–220 LOC if ingest is cut), but it is **not** "just timing calls." The three things that must not be compromised: **(1)** the middleware must be pure-ASGI so the ContextVar actually propagates (a `BaseHTTPMiddleware` here silently produces empty timings and is the most likely way to ship a broken version); **(2)** `record_stage` must be a true no-op when unbound, or the eval harness and the library-import contract break; **(3)** the correlation ID must reach telemetry **without** opening any door for raw query text — verify in review that the factories still forbid a `query` kwarg. Get those right and B1 turns every later ranking change (B3, B4, the Phase C HyDE/RAG-Fusion work) from "feels faster" into a number with a story.
