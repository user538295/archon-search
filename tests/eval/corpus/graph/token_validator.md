# TokenValidator

TokenValidator verifies JWT tokens and validates token expiry.
It is called by the authentication pipeline to confirm token integrity before granting access.
TokenValidator works with AuthService to enforce authentication policies.
