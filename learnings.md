# Learnings

## What Has Failed
- **[2026-07-31] (×13) pytest OOM / parallelism**: `-n auto` on real-model lanes OOM-crashed the 48GB machine; use `-n 8` (stubs ~0.3GB/worker, never `-n auto`/`-n0`). Clear check: `ps -Ao comm=,args= | awk '$1 ~ /[Pp]ython/ && /bin\/pytest/'`; full suite always backgrounds → subagents run scoped foreground, coordinator runs full suite once at end.
- **[2026-07-05] (×3) implement-all**: no-bundle (temp defaults task 1, thread callers later); fix-agent prompts ≤5 targeted fixes; recovery-check violations emit full What/Why/Fix/Prevention block; `git status --porcelain` at 100%.

## What Has Worked
- **[2026-08-02] (×17) Backlog "bug" briefs**: run existing reproducing test first — may already be fixed. Fix-at-route ≠ fully fixed (verify full request path AND every process entry point — S07 `_maybe_save_config` no-op'd because `run_server` never threaded `config_path` to `create_app`; both `__main__.py` and `serve.py` dropped it). Report fixed-with-SHA. Net-new = ONE assertion in strongest existing test. "Still listed"=weak proxy. Dry-run repro: existing tests stub the destructive fn AND point paths at non-existent files → vacuous; repro needs a REAL existing file + un-stubbed fn (install.py Step-0 `_remove_legacy_service` had no dry_run guard).
- **[2026-07-31] (×17) regression-guard files**: `path_home_allowlist.txt` pins (file,lineno,sha) — shifts on any added line above callsite. OpenAPI snapshot: regen with `--update-openapi-snapshot` on 3.12. New SearchConfig field: dataclass+`_apply_toml`+coerce+snapshot tests. `response_model=None` trips `test_no_empty_schemas_remain`.
- **[2026-07-31] (×11) CLI HTTP-proxy conversion**: grep `tests/` for all patches of removed symbols first; mock `httpx.get/post/delete` (patch where USED). `ConnectError` before `HTTPError`. Test env key must be 64-char hex. Non-terminal `.get(field, sentinel)` in a `while` loop = silent hang → explicit None + fail-fast.
- **[2026-07-21] (×10) adding a column to schema/CollectionMeta**: mirror ALL sites: dataclass, `_row_to_meta`, BOTH meta-write fns, all ctor sites, `_ROUTING_FIELDS` (omit job-id). Still needs `_migrate_<field>()` wired + catalog entry, even if no STORE_SCHEMA_VERSION bump.
- **[2026-07-17] (×9) job-spawning REST route**: guard→404→create→`transition({QUEUED},RUNNING)` BEFORE `create_task`→`_background_tasks`+`add_done_callback`. 409 via persisted `meta.<job>_job_id` lazy-stale-clear. Declare `response_model=`+`responses=` or snapshot documents empty `{}`.
- **[2026-07-29] (×6) collection visibility = meta-in-namespace**: gate on `get_collection_meta(name,ns) is not None` (not config only); list unions config+ns_names. Bare `MagicMock` on `get_collection_meta` → stub as `AsyncMock(return_value=None)`. `POST /route` root cause: pass store metas as `initial_metadata` into router.
- **[2026-07-05] (×6) xdist/asyncio/stub**: module-level stubs must restore immediately; absent-pkg simulation via `monkeypatch.setitem` is vacuous if already imported → subprocess or del+reload. `asyncio.run()` not `get_event_loop()`. `async def` → `AsyncMock`. MCP tests need `xdist_group("mcp")`.
- **[2026-07-21] (×6) test vacuity**: `assert a in x or b in x` → separate asserts. Guard loops with `assert results` first. Exception-path tests need `assert_awaited_once()`. Override tests start at OPPOSITE value. `MagicMock(name=...)` sets display name, not `.name`.
- **[2026-07-31] (×3) Strategy-split a dry_run god-class (install.py)**: extract EVERY run() mutation into a self-narrating `@abstractmethod` (Real executes, Dry prints "Would…"), called unconditionally so run() holds no raw mutation; non-mutation dry branches (prompt/wait/msgs) stay. Real guarantee = a backstop test: full dry-run fails if any seam (`subprocess.run`/`atomic_write_bytes`/`copy2`/`rmtree`/`os.chmod`) fires — the ABC alone can't catch inline slips. Factory `create_installer`; patch read-only on Base, destructive on the concrete matching construction.
- **[2026-07-31] (×2) Split a module the suite deep-patches via `patch("pkg.X")`**: install.py 2857L→10-module pkg DONE, suite green. Re-exporting `__init__` keeps every import; retarget `patch("pkg.X")` strings to the REAL call-site module (hub-called→`installer`, leaf-called deps→that leaf e.g. force-reinstall's→`prewarm`). Stdlib-attr (`os`/`shutil`) & class-method (`RealInstaller.m`) patches survive IF `__init__` imports those stdlib mods. `patch.multiple("pkg",...)`+prefix-strip helpers: repoint BOTH target module AND the `.replace()` prefix. `errors.py` breaks cycles; delegate the mechanical move to a subagent, drive the test-fix loop yourself.
- **[2026-07-06] (×5) LanceDB quirks**: `.limit()` is scan limit not sort-then-limit. `merge_insert().when_matched_update_all()` replaces whole row. Missing table → `ValueError("Table not found")`. `count_rows(filter=)`. `db.close()` sync. SQL via `_where_eq`/`_where_in` never f-strings.

- **[2026-08-02] (×1) worktree + half-wired params**: in a git worktree, bash `cd /Users/.../archon-search` reads the SHARED checkout not the worktree — use default cwd (no cd) so Read/Edit/git hit the worktree. A new fn param no caller passes is a smell: complete the wiring (serve.py→run_server config_path) not just internal-default resolution; that changing `run_server(cfg)`→`run_server(cfg,path)` breaks `test_cli_serve` `assert_called_once_with(cfg)` + `_capture(cfg)` side_effects — update both. Strip subagent scope-creep (a DATA_DIR re-derive added only to paper over a test) — fix the test's own db_path isolation instead.

## Plan-Making & Agent Process
- **[2026-08-02] (×23) verify before acting**: `git log --oneline -- <handler> <test>` + run existing test BEFORE any fix. DA/reviewer "Critical/Major" labels need grep-verification (in a doc review, verify every cited file:line against source — plan claims were ~all accurate but a few were stale). Verify methods/CLI options exist (Read the source). Non-terminal sentinel in `while` loop = silent hang → explicit None + fail-fast.
- **[2026-07-31] (×16) subagent lifecycle & handoff**: Explore subagents lack Write tool → return findings inline → orchestrator writes them to files immediately. Session-limit kills late agents — check if partial file was written. Subagents: scoped tests foreground only; coordinator: full suite once at end. Fix-agents silently add unrequested changes (e.g. embed review-ID tags RCn/finding-X into deliverable prose → dangling refs) → diff against literal finding list + final strip pass.
- **[2026-07-30] (×8) smoke/subprocess patterns**: use `-o addopts=` not `-p no:xdist`. Session-scoped fixtures use `tmp_path_factory`. `GET /collections` (no slash) → 307. Pair `ARCHON_SEARCH_CONFIG` with DATA_DIR. Seed real text (empty → 0 chunks). `xdist_group("smoke_e2e")` serialises EXECUTION not fixture lifetime.
- **[2026-07-30] (×4) TypeSpec contracts**: `field?: T | null` for optional nullable. `namespace`/`model`/`op`/`enum` reserved — use `ns`. Cross-file refs fail — repeat locally. `tsp compile <f> --no-emit` verifies; `--emit @typespec/openapi3` regenerates yaml. `tsp-output/` may be git-tracked.
- **[2026-07-29] (×7) doc close-out**: update in dependency order (api-reference → component-catalog → CLAUDE.md → user manual). Grep WHOLE docs tree for old invariant strings — per-file scope orphans siblings. Verify example output against production fn. BREAKING.md: no entry for additive-only.
- **[2026-07-31] (×2) skill authoring & availability**: skills live at `~/.claude/skills/<name>/SKILL.md` (YAML frontmatter `name`+`description`); orchestration skills chain sub-skills via the Skill tool (`commit`, `iterative-review`) and spawn work via Agent tool. Availability check: system-reminder listing is PRIMARY, `ls ~/.claude/skills/<name>/` fallback — OR not AND; a freshly-written SKILL.md appears in the listing same-turn.
- **[2026-07-31] (×5) plan/tasks iterative-review**: one pass reconciles contracts drifted from resolved decisions. Pin resolved IDs for parallel fix-agents. Over-specified scenario recipes → per-cycle "finding-generators" → downscope to regression-guard + note. ASGI lifespan `finally` runs after uvicorn closes its socket → 503 window unobservable by new connections (subprocess-spike first). "Banner all superseded X" recurs across cycles when spots are hand-enumerated → do one grep-exhaustive sweep incl. mermaid/diagram nodes (text sweeps miss visuals).
