# Learnings

## What Has Failed
- **[2026-08-07] (×19) pytest OOM / parallelism**: `-n auto` on real-model lanes OOM-crashed the 48GB machine; use `-n 8` (stubs ~0.3GB/worker, never `-n auto`/`-n0`). Clear check: `ps -Ao comm=,args= | awk '$1 ~ /[Pp]ython/ && /bin\/pytest/'`; full suite always backgrounds → subagents run scoped foreground, coordinator runs full suite once at end.
- **[2026-07-05] (×3) implement-all**: no-bundle (temp defaults task 1, thread callers later); fix-agent prompts ≤5 targeted fixes; recovery-check violations emit full What/Why/Fix/Prevention block; `git status --porcelain` at 100%.

## What Has Worked
- **[2026-08-04] (×2) S192 uvicorn JSON-log leak**: `run_server` `uvicorn.run(app,host,port)` w/ NO `log_config` → uvicorn's TEXT `LOGGING_CONFIG` (propagate=False) leaks plain-text; Fix: `build_uvicorn_log_config`=`copy.deepcopy(uvicorn.config.LOGGING_CONFIG)`, ADD `json` formatter + repoint every handler — do NOT replace whole `formatters` dict: uvicorn indexes `formatters['default'|'access']` to inject `use_colors`.
- **[2026-08-07] (×38) Backlog "bug" briefs**: run/write reproducing test FIRST — may already be fixed; grep git log for the bug ID before trusting an uncommitted "fix". Env non-defect pattern: add 409s on CONFIG membership but search/reindex 404 on STORE meta — divergent stores. For a DOC bug the failing test is a doc-consistency scan grounded by a wire-truth/behavioral assert. Scope scan to exact subsection. Fix at the right LAYER, verify full path AND every peer call-site: S97 ingest route dropped collection/path; S07 `run_server` never threaded `config_path`. S272: a merged+"approved" fix can target the WRONG root cause — always independently re-derive repro conditions before trusting a "0 issues" review. S340: `/explain` and `/search` have SEPARATE fanout blocks in `pipeline.py` (~L1775 and ~L2588) — fixing one does NOT fix the other.
- **[2026-08-07] (×23) regression-guard files**: `path_home_allowlist.txt` pins (file,lineno,sha) — shifts on added line above callsite; TWO update sites. OpenAPI snapshot: on py3.13 `http.HTTPStatus(422).phrase`=`"Unprocessable Content"` (renamed; CI pins 3.12) — canonicalise in `_strip_dynamic_fields` instead of regen; confirm any snapshot failure pre-exists via `git stash -u`. `response_model=None` trips `test_no_empty_schemas_remain`. release.sh gate: guard with `RELEASE_SH_TEST_MODE` AND `DRY_RUN!=1`. When renaming an error-body key (e.g. `"error"`→`"code"`), `grep -rn '"error".*store_busy' tests/` before running the suite.
- **[2026-08-07] (×12) CLI HTTP-proxy conversion**: grep `tests/` for all patches of removed symbols first; mock `httpx.get/post/delete` (patch where USED). `ConnectError` before `HTTPError`. Test env key must be 64-char hex. Non-terminal `.get(field, sentinel)` in a `while` loop = silent hang → explicit None + fail-fast.
- **[2026-07-21] (×10) adding a column to schema/CollectionMeta**: mirror ALL sites: dataclass, `_row_to_meta`, BOTH meta-write fns, all ctor sites, `_ROUTING_FIELDS` (omit job-id). Still needs `_migrate_<field>()` wired + catalog entry, even if no STORE_SCHEMA_VERSION bump.
- **[2026-08-02] (×10) job-spawning REST route**: guard→404→create→`transition({QUEUED},RUNNING)` BEFORE `create_task`→`_background_tasks`+`add_done_callback`. 409 via persisted `meta.<job>_job_id` lazy-stale-clear. Declare `response_model=`+`responses=` or snapshot documents empty `{}`. `dry_run` preview must skip the `meta.<job>_job_id`+`update_collection_meta` write.
- **[2026-07-29] (×6) collection visibility = meta-in-namespace**: gate on `get_collection_meta(name,ns) is not None` (not config only); list unions config+ns_names. Bare `MagicMock` on `get_collection_meta` → stub as `AsyncMock(return_value=None)`. `POST /route` root cause: pass store metas as `initial_metadata` into router.
- **[2026-08-07] (×7) xdist/asyncio/stub**: module-level stubs must restore immediately; absent-pkg simulation via `monkeypatch.setitem` is vacuous if already imported → subprocess or del+reload. `asyncio.run()` not `get_event_loop()`. `async def` → `AsyncMock`. MCP tests need `xdist_group("mcp")`. `importlib.reload(archon_search.rag_fusion)` mints a NEW error class — routes_search import-time binding goes stale. Never reload a prod module others import symbols FROM — patch the module attr instead.
- **[2026-07-21] (×6) test vacuity**: `assert a in x or b in x` → separate asserts. Guard loops with `assert results` first. Exception-path tests need `assert_awaited_once()`. Override tests start at OPPOSITE value. `MagicMock(name=...)` sets display name, not `.name`.
- **[2026-08-03] (×7) LanceDB quirks**: `.limit()` is scan limit not sort-then-limit. `merge_insert(["col1","col2"])` for composite key. NULL-namespace legacy rows won't match explicit "default" key, delete them AFTER merge_insert. `merge_insert().when_matched_update_all()` replaces whole row. Missing table → `ValueError("Table not found")`. `db.close()` sync. SQL via `_where_eq`/`_where_in` never f-strings.
- **[2026-08-04] (×8) install/wizard bugfix gotchas**: Docker version: `.git` dockerignored → hatch-vcs stamps `0.0.0+local` → pass `RELEASE_VERSION` build-arg + `SETUPTOOLS_SCM_PRETEND_VERSION`. In a git worktree, `cd /Users/.../archon-search` reads SHARED checkout — use default cwd. New fn param no caller passes = complete the wiring. S269 CPU torch: pin torch+torchvision to `[[tool.uv.index]] pytorch-cpu` + `[tool.uv.sources]` marker=`linux x86_64` — uv applies sources only to DIRECT deps. Regen with plain `uv lock`, NEVER `--upgrade`. S02: `_wait_for_service` fixed budget + first-launch import crash + launchd restart = false "timed out"; detect `ModuleNotFoundError`/`ImportError` in log, extend deadline ONCE.

## Plan-Making & Agent Process
- **[2026-08-07] (×44) verify before acting**: pre-reproduce: `ls Documentation/Completed/ | grep <ID>` + `git log --oneline | grep <ID>` FIRST. Non-vacuity: `git stash push -- <prod-file>` → new tests MUST fail → `git stash pop`; handler test injecting into `MagicMock` can't guard a wiring bug; use real e2e + structural guard. On /commit, `diff` stray ` copy.md` artifacts vs tracked originals before trashing. DA/reviewer "Critical/Major" labels need grep-verification. Non-terminal sentinel in `while` loop = silent hang → explicit None + fail-fast.
- **[2026-08-07] (×26) subagent lifecycle & handoff**: fix-agents regenerate openapi_snapshot.json unasked — `git status` after every fix pass. Explore subagents lack Write tool → return findings inline → orchestrator writes immediately. bugfix/reviewer subagents repeatedly go IDLE after delegating to children → coordinator must TaskStop idle agent and drive suite→iterative-review→commit itself. `TaskStop` from inside forked/nested context fails → proceed directly; check `git status --porcelain` first since stopped agent may have written correct files. Agents self-report premature "completed" after just polling a sub-agent — resume with firm directive.
- **[2026-08-04] (×9) smoke/subprocess patterns**: use `-o addopts=` not `-p no:xdist`. Session-scoped fixtures use `tmp_path_factory`. `GET /collections` (no slash) → 307. Pair `ARCHON_SEARCH_CONFIG` with DATA_DIR. Seed real text (empty → 0 chunks). `xdist_group("smoke_e2e")` serialises EXECUTION not fixture lifetime.
- **[2026-08-07] (×6) TypeSpec contracts**: `field?: T | null` for optional nullable. `namespace`/`model`/`op`/`enum` reserved — use `ns`. Cross-file refs fail — repeat locally + "SYNCHRONIZATION" comment. `tsp compile <f> --no-emit` verifies. A K1 "decision" resolving a contract ambiguity must be checked against the WHOLE plan/tasks text. Adding a field with no default to a real Python `@dataclass` where every existing field has one raises `TypeError` at import time.
- **[2026-08-07] (×14) doc close-out**: update in dependency order (api-reference → component-catalog → CLAUDE.md → user manual). Grep WHOLE docs tree for old invariant strings. BREAKING.md: no entry for additive-only, but DO add one when a core dep becomes optional extra. `Documentation/Completed/` archives are off-limits for editing; cleanup (trash) of stale Completed/ files is explicitly sanctioned when superseded.
- **[2026-08-07] (×2) async timing TOCTOU**: (1) `_default_ingest_task_with_lock` releases lock BEFORE `_dispatch_ingest` → free lock ≠ safe to delete; use job-store RUNNING/CANCELLING status. (2) `_rotate()` in `_completion_loop` 60s poll → test sees job DONE before rotation fires; fix: call `_bl._drain_completed()` synchronously in wrapper.
- **[2026-08-07] (×3) iterative-review non-convergence**: nested DA/Brooks sub-agents silently stall past 15+ min — after 2-3 wait cycles, review the diff directly (`git show`+Read+Grep) instead. Adding a value to an allowlist without updating every consuming factory → silent wrong-provider fallback — grep consumers. Task-breakdown bullet lists can omit requirements stated only in the plan's prose "Done when" line.
- **[2026-08-07] (×10) plan/tasks iterative-review**: pin resolved IDs for fix-agents. `app.py` lifespan closure line# < body line# but executes AFTER (body runs at create_app, closure at startup). Privacy trilemma: resolve at policy layer. Text sweeps miss diagram nodes.
