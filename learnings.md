# Learnings

## What Has Failed
- **[2026-08-08] (×17) pytest OOM / parallelism**: `-n auto` on real-model lanes OOM-crashed the 48GB machine; use `-n 8` (stubs ~0.3GB/worker, never `-n auto`/`-n0`). Clear check: `ps -Ao comm=,args= | awk '$1 ~ /[Pp]ython/ && /bin\/pytest/'`; full suite always backgrounds → subagents run scoped foreground, coordinator runs full suite once at end.
- **[2026-07-05] (×3) implement-all**: no-bundle (temp defaults task 1, thread callers later); fix-agent prompts ≤5 targeted fixes; recovery-check violations emit full What/Why/Fix/Prevention block; `git status --porcelain` at 100%.

## What Has Worked
- **[2026-08-07] (×1) sync.py make_embedder trap**: when `meta.active_embedding_model == global_embedder.model_name`, skip `make_embedder()` and reuse the global — stubs intercept any model name and return 384-dim, causing schema mismatch on a 4-dim test table; guard: `if not _active_model or _active_model == _global.model_name: use _global`.
- **[2026-08-04] (×2) S192 uvicorn JSON-log leak**: `run_server` `uvicorn.run(app,host,port)` w/ NO `log_config` → uvicorn's TEXT `LOGGING_CONFIG` (propagate=False) leaks `INFO:     Started server process` plain-text; launchd plist StdOut+StdErr→JSON file → interleave. Repro: patched-`uvicorn.run` fake runs REAL `uvicorn.Config(app,**kw).configure_logging()`+`getLogger("uvicorn.error").info(...)`, redirect stdout/stderr→file BEFORE configure_logging; assert every line JSON. Fix: `build_uvicorn_log_config`=`copy.deepcopy(uvicorn.config.LOGGING_CONFIG)`, ADD a `json` formatter + repoint every handler, set every logger `level=config.level` — do NOT replace the whole `formatters` dict: uvicorn's `configure_logging` indexes `formatters['default'|'access']` to inject `use_colors` → `KeyError` if deleted (dormant till a caller passes use_colors). Derive (not hardcode logger list) so new/renamed uvicorn loggers auto-covered; guard that property with a synthetic-extra-logger test patching `LOGGING_CONFIG`. uvicorn lines carry no correlation_id & access fields collapse into `message` (doc it).
- **[2026-08-08] (×36) Backlog "bug" briefs**: run/write reproducing test FIRST — may already be fixed; grep git log for the bug ID before trusting an uncommitted "fix" — it can duplicate an already-committed one (S194 wizard timeout was fixed by b8a7949c removing legacy-service cleanup = plist-removal→port-8765-race→launchd-ThrottleInterval 60s; a 2nd uncommitted skip_preload→`_compute_svc_timeout` patch was a false-premise no-op: /health is static + eager-load off = download never gates readiness, and 220MB*100//1000=22<60 floor so the timeout was unchanged; its smoke test was false-green only because it compressed the 60 floor to 3, inverting the max()). Env non-defect pattern: add 409s on CONFIG membership (routes_collections.py:187) but search/reindex 404 on STORE meta (routes_search.py:297) — divergent stores; smoke "add 409 + search 404" repro ONLY under dirty cross-run state (config persists, store reset), prove via clean-state happy-path e2e (passes) + seeded-divergence repro — DON'T patch a correct dedup. For a DOC bug the failing test is a doc-consistency scan (grep the file for the wrong literal) grounded by a wire-truth/behavioral assert (S168 lowercase `ok/warn/fail/pending`; S186 body-required→422-before-409; S59/S117 doc omitted a real precondition/envelope — assert against the code that emits it). Scope scan to exact subsection (`content[content.index("### `naive`"):...]`) — whole-file `in` is vacuous when literal recurs. Verify doc↔code truth before product-vs-doc fix (S115: schema dropped 2 promised fields → product; S133: docs correct, code was the bug; S51: server already honored X-Ingested-By the CLI omitted → product, send header; S60: docs + N sibling paths agree, one outlier path + its test disagree → outlier is the bug → PRODUCT, flip the contradicting test(s) too). Fix at the right LAYER, not just the route — verify full path AND every peer call-site / process entry point: S97 ingest route dropped collection/path in store.create(); S07 `_maybe_save_config` no-op'd because `run_server` never threaded `config_path` to `create_app` (`__main__.py`+`serve.py` both dropped it); S133 `rotate_default_key` didn't synth a grace record for the unmanaged .search.env default key (guard resurrection with a "seen-any-record" flag). Net-new = ONE assertion in strongest existing test. "Still listed"=weak proxy. Dry-run repro: stub+nonexistent-path = vacuous; needs a REAL existing file + un-stubbed fn (install.py Step-0 `_remove_legacy_service` had no dry_run guard). Don't touch `Documentation/Completed/` archives.
- **[2026-08-04] (×21) regression-guard files**: `path_home_allowlist.txt` pins (file,lineno,sha) — shifts on any added line above callsite; TWO update sites: the allowlist file AND the `_EXPECTED_CONFIG_LINE_NO` constant in the test — both must move together. OpenAPI snapshot: regen with `--update-openapi-snapshot` on 3.12 (FastAPI version bumps change 422 description string). New SearchConfig field: dataclass+`_apply_toml`+coerce+snapshot tests. `response_model=None` trips `test_no_empty_schemas_remain`. release.sh test gate: guard with `RELEASE_SH_TEST_MODE` AND `DRY_RUN!=1` — tests run release.sh in temp repos with 0 test files → pytest exits 5 → bail fires.
- **[2026-07-31] (×11) CLI HTTP-proxy conversion**: grep `tests/` for all patches of removed symbols first; mock `httpx.get/post/delete` (patch where USED). `ConnectError` before `HTTPError`. Test env key must be 64-char hex. Non-terminal `.get(field, sentinel)` in a `while` loop = silent hang → explicit None + fail-fast.
- **[2026-07-21] (×10) adding a column to schema/CollectionMeta**: mirror ALL sites: dataclass, `_row_to_meta`, BOTH meta-write fns, all ctor sites, `_ROUTING_FIELDS` (omit job-id). Still needs `_migrate_<field>()` wired + catalog entry, even if no STORE_SCHEMA_VERSION bump.
- **[2026-08-02] (×10) job-spawning REST route**: guard→404→create→`transition({QUEUED},RUNNING)` BEFORE `create_task`→`_background_tasks`+`add_done_callback`. 409 via persisted `meta.<job>_job_id` lazy-stale-clear. Declare `response_model=`+`responses=` or snapshot documents empty `{}`. `dry_run` preview must skip the `meta.<job>_job_id`+`update_collection_meta` write (non-atomic delete→add opens a 404 window for lock-free `get_collection_meta`) — task finally already no-ops since job-id never set (S52).
- **[2026-07-29] (×6) collection visibility = meta-in-namespace**: gate on `get_collection_meta(name,ns) is not None` (not config only); list unions config+ns_names. Bare `MagicMock` on `get_collection_meta` → stub as `AsyncMock(return_value=None)`. `POST /route` root cause: pass store metas as `initial_metadata` into router.
- **[2026-07-05] (×6) xdist/asyncio/stub**: module-level stubs must restore immediately; absent-pkg simulation via `monkeypatch.setitem` is vacuous if already imported → subprocess or del+reload. `asyncio.run()` not `get_event_loop()`. `async def` → `AsyncMock`. MCP tests need `xdist_group("mcp")`.
- **[2026-07-21] (×6) test vacuity**: `assert a in x or b in x` → separate asserts. Guard loops with `assert results` first. Exception-path tests need `assert_awaited_once()`. Override tests start at OPPOSITE value. `MagicMock(name=...)` sets display name, not `.name`.
- **[2026-08-07] (×2) 3rd-party mass version bump**: reproduce PER-LANE (default+live_benchmark+smoke+docling — all green after existing working-tree fixes). FastAPI 0.141 wraps included routers in `_IncludedRouter` (no `.path`) → when scanning `app.routes` recurse via `.original_router.routes`; `get_openapi` handles it internally. pyarrow 25 SIGSEGVs on macOS/arm64 in mimalloc `mi_thread_init` (attribute via `~/Library/Logs/DiagnosticReports/Python-*.ips`, faulting lib=libarrow) → `os.environ.setdefault("ARROW_DEFAULT_MEMORY_POOL","system")` darwin-gated in empty `archon_search/__init__.py` (pyarrow/lancedb are lazy → runs before any Arrow import in EVERY entry point) + `tests/conftest.py`; subprocess guard test proves __init__ pins it. lancedb 0.36 dropped stable tied-row order → the zero-vector fastembed stub (chonkie+reranker also stubbed) ties ALL scores → boundary/ranking integration tests flake (~40%); fix test-LOCAL via `monkeypatch` deterministic content-aware embed (auto-reverts, zero blast radius) + concept-axis-separated corpus so the leftover is a full gap not a tie-break — NOT a shared-stub change. Forks INHERIT context: a segfault-heavy context made a fork ignore its redirect twice and emit an unrequested 41k audit doc off "/aaa" phrasing → TaskStop + take over; give narrow forks a clean scoped prompt, verify their factual claims yourself.
- **[2026-08-03] (×7) LanceDB quirks**: `.limit()` is scan limit not sort-then-limit. `merge_insert(["col1","col2"])` for composite key — closes delete→add 404 window; NULL-namespace legacy rows won't match explicit "default" key, so delete them AFTER merge_insert (canonical row already exists → no gap). `merge_insert().when_matched_update_all()` replaces whole row. Missing table → `ValueError("Table not found")`. `count_rows(filter=)`. `db.close()` sync. SQL via `_where_eq`/`_where_in` never f-strings.
- **[2026-08-04] (×8) install/wizard bugfix gotchas**: Docker version: `.git` is dockerignored so hatch-vcs stamps `0.0.0+local` → pass `RELEASE_VERSION` build-arg + `SETUPTOOLS_SCM_PRETEND_VERSION=$VER+docker` before `pip install .` (PRETEND short-circuits `no-local-version`, keeps `+docker`); release.yml passes `$TAG`, compose passes `${ARCHON_VERSION:-0.0.0}`. In a git worktree, `cd /Users/.../archon-search` reads the SHARED checkout — use default cwd (no cd). New fn param no caller passes = smell: complete the wiring (serve.py→run_server config_path; register() config_path) + fix tests asserting old arity. GraphNode: `make_code_symbol_qualified_name` bakes source_path into the id — `source_path=None` same-name nodes collide; build with explicit id for legacy pre-S68 rows. S269 CPU torch (RIGHT fix): torch enters ONLY via docling→docling-slim→docling-ibm-models (fastembed/reranker use onnxruntime). Keep docling CORE; pin torch+torchvision to `[[tool.uv.index]] pytorch-cpu`+`[tool.uv.sources]` marker=`linux x86_64` — uv applies sources only to DIRECT deps so declare them directly (bounds mirror docling). Regen with plain `uv lock`, NEVER `--upgrade` (bumps 200+ pkgs → eval-baseline+fastapi+graph fail). Offline guard: tomllib-walk uv.lock closure (seed core+[graph,code,multilingual], honor linux markers) has no `nvidia-*/cuda*`, allow `+cpu` torch by name (legit). Wizard CUDA swap: public `==2.13.0` IS satisfied by installed `2.13.0+cpu` (PEP440 ignores local label)→pip skips→silent no-op; pin exact LOCAL `==<ver>+cu126` (tag parsed from index URL) to force reinstall, `--extra-index-url` keeps PyPI for transitive deps, best-effort catch `OSError` (uv non-exec/wrong-arch) not just FileNotFoundError; gate linux/x86_64 only. first-start network-bound → widen HEALTHCHECK start-period. S02 readiness gate: `_wait_for_service` fixed budget + first-launch import crash + launchd restart = false "timed out"; detect `ModuleNotFoundError`/`ImportError` (startswith) in log from a byte-offset snapshotted BEFORE (re)start, extend deadline ONCE, stay gated on 200. S194/S201: service plist/unit must propagate ARCHON_SEARCH_DATA_DIR alongside ARCHON_SEARCH_CONFIG; tests using `patch.object(Path,'home',return_value=tmp)` break when code switches to `get_data_dir()` — also `monkeypatch.setenv('ARCHON_SEARCH_DATA_DIR',str(tmp/'.archon-search'))`. S202 prewarm: reranker failure must be non-fatal (`logger.warning`, continue); only embedder failure is fatal — the server starts without a cached reranker and downloads it on first search.

## Plan-Making & Agent Process
- **[2026-08-08] (×39) verify before acting**: pre-reproduce: `ls Documentation/Completed/ | grep <ID>` + `git log --oneline | grep <ID>` FIRST — both hit = bug closed, stop. `git log --oneline -- <handler> <test>` + run existing test BEFORE any fix. On /commit, `diff` stray ` copy.md` artifacts vs tracked originals before trashing; exclude net-new reports (e.g. S269) from a backlog→Completed move commit. DA/reviewer "Critical/Major" labels need grep-verification (in a doc review, verify every cited file:line against source — plan claims were ~all accurate but a few were stale). Verify methods/CLI options exist (Read the source). Non-terminal sentinel in `while` loop = silent hang → explicit None + fail-fast. `ls` before trash to confirm which timestamped vs bare-named Completed/ variants exist.
- **[2026-08-08] (×23) subagent lifecycle & handoff**: Explore subagents lack Write tool → return findings inline → orchestrator writes them to files immediately. Session-limit kills late agents — check if partial file was written. Subagents: scoped tests foreground only; coordinator: full suite once at end. Fix-agents silently add unrequested changes (e.g. embed review-ID tags RCn/finding-X into deliverable prose → dangling refs) → diff against literal finding list + final strip pass. bugfix/reviewer subagents repeatedly go IDLE after delegating to their own children without finishing (suite/review/commit) → coordinator must TaskStop the idle agent (prevents concurrent pytest = OOM) and drive suite→iterative-review→commit itself; "correctness"-angle DA agents often idle without SendMessage-ing findings → poke via SendMessage or read their output file.
- **[2026-08-04] (×9) smoke/subprocess patterns**: use `-o addopts=` not `-p no:xdist`. Session-scoped fixtures use `tmp_path_factory`. `GET /collections` (no slash) → 307. Pair `ARCHON_SEARCH_CONFIG` with DATA_DIR. Seed real text (empty → 0 chunks). `xdist_group("smoke_e2e")` serialises EXECUTION not fixture lifetime.
- **[2026-08-04] (×5) TypeSpec contracts**: `field?: T | null` for optional nullable. `namespace`/`model`/`op`/`enum` reserved — use `ns`. Cross-file refs fail — repeat locally. `tsp compile <f> --no-emit` verifies; `--emit @typespec/openapi3` regenerates yaml. `tsp-output/` may be git-tracked.
- **[2026-08-03] (×12) doc close-out**: update in dependency order (api-reference → component-catalog → CLAUDE.md → user manual). Grep WHOLE docs tree for old invariant strings — per-file scope orphans siblings. Verify example output against production fn. BREAKING.md: no entry for additive-only, but DO add one when a core dep becomes an optional extra. `Documentation/Completed/` archives are off-limits for editing; cleanup (trash) of stale Completed/ files is explicitly sanctioned when their content is superseded by code/tests.
- **[2026-07-31] (×2) skill authoring & availability**: skills live at `~/.claude/skills/<name>/SKILL.md` (YAML frontmatter `name`+`description`); orchestration skills chain sub-skills via the Skill tool (`commit`, `iterative-review`) and spawn work via Agent tool. Availability check: system-reminder listing is PRIMARY, `ls ~/.claude/skills/<name>/` fallback — OR not AND; a freshly-written SKILL.md appears in the listing same-turn.
- **[2026-08-05] (×9) plan/tasks iterative-review**: pin resolved IDs for fix-agents. `app.py` lifespan closure line# < body line# but executes AFTER (body runs at create_app, closure at startup — proved by `_graph_store` injection pattern; never infer construction order from raw line numbers). Privacy trilemma: resolve at policy layer (entity names ≠ raw input text → guard scopes to input vars only). Text sweeps miss diagram nodes.
