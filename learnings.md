# Learnings

## What Has Failed
- **[2026-08-14] (×40) pytest OOM / parallelism**: `-n auto` on real-model lanes OOM-crashed the 48GB machine; use `-n 8` (stubs ~0.3GB/worker, never `-n auto`/`-n0`). Pre-flight check: `ps -Ao args= | grep -E '[/]bin/pytest|[u]v run pytest'` — the `$1 ~ /[Pp]ython/` comm-column variant (incl. CLAUDE.md's) FALSE-NEGATIVES (macOS truncates comm to 15 chars → `/opt/homebrew/Ce`), reporting "no pytest running" mid-suite. Full suite always backgrounds → subagents run scoped foreground, coordinator runs full suite once at end. Wall-clock-budget tests flake under contention (a peer agent's suite stretched a run 159s→301s): `test_e2a_t4_scope_wildcard_benchmark` (p99<10 ms) and `test_warmup_does_not_block_lifespan_startup` each failed once in DIFFERENT full runs, pass in isolation — a different timing test failing per run = contention, not regression. Fix when the budget elapses ONLY on the failure path (an `asyncio.wait(timeout=)` guarding an event that a correct impl sets immediately): raise it 5s→30s — free on green, kills the flake. Done 2026-08-14.
- **[2026-07-05] (×3) implement-all**: no-bundle (temp defaults task 1, thread callers later); fix-agent prompts ≤5 targeted fixes; recovery-check violations emit full What/Why/Fix/Prevention block; `git status --porcelain` at 100%.

## What Has Worked
- **[2026-08-04] (×2) S192 uvicorn JSON-log leak**: `run_server` `uvicorn.run(app,host,port)` w/ NO `log_config` → uvicorn's TEXT `LOGGING_CONFIG` (propagate=False) leaks plain-text; fix: `build_uvicorn_log_config`=`copy.deepcopy(uvicorn.config.LOGGING_CONFIG)`, ADD a `json` formatter + repoint every handler, set every logger `level=config.level` — do NOT replace the whole `formatters` dict: uvicorn's `configure_logging` indexes `formatters['default'|'access']` to inject `use_colors` → `KeyError` if deleted.
- **[2026-08-12] (×53) Backlog "bug" briefs**: A passing regression test named for a bug ID may cover only ONE branch of the guard — S483's existing test set `indexed_chunk_size=512`, but the `!= 0` sentinel in `sync._check_collection_changes` silently skips the guard when state is EMPTY (HTTP/job ingest writes no sync state). Root fix: `CollectionProgress.indexed_chunk_size: int|None=None`; guard branches on `is None` (unknown prior → reindex if flag); call site: `force_reindex and (indexed_cs is not None or chunk_count>0)` so genuine-change path (known `indexed_cs`) works even if store mock returns `chunk_count=0`; both `sync()` and `sync_collection()` share the same guard. Call-site proxy (`not file_mtimes and flag`) is wrong — fires for corrupt JSON, deleted+restored files, empty dirs; always fix at the guard layer. Always test the zero/absent-state fallback branch too. S284 had TWO reports: S284-documented_behavior (doc gap) + S284-collection_remains_registered_with_docs_zero (code never implemented the behavior). Doc-only fix ≠ behavioral fix — always check code after closing a DOC-tagged report. S272 reproduces ONLY with `[rag_fusion] enabled=false` — probe every config permutation before writing the repro. A re-filed brief with the SAME filename already in `Completed/` = regression or second failure mode. S272 definitively closed by commit 2d39d977; a follow-up wrongly asserted `rag_fusion_attempted=True` with `rag_fusion_enabled=False` — contradicting the contract; correct action: remove wrong test. Two briefs can be complementary halves of one contract — S561 "wizard must not write default keys" + S104/S167 "`config show` must display them" resolve by making `config show` merge `_default_toml()` into parsed file; never route via `load_config` (breaks troubleshooting step). run/write reproducing test FIRST. pre-reproduce: `git log --oneline -- <handler> <test>` BEFORE any fix; a "fix pre-existing test failures" commit can revert a shipped fix and invert its regression test — S561's `248bae1b` did exactly this. A green test proves only that code and test agree — diff the test against the DOC contract too. Docstring says "key IS written" + assertion says "key absent" = misleading test — always decide the behavior and update both to agree. Making a wizard key absent (formerly always written) regresses `config get` for that key — test `config get <key>` in the same PR; fix by switching `config get` to `_effective_toml`. Widening fn return N→(N+1) requires updating `MagicMock(return_value=(...))` and `patch.object(return_value=...)` in tests — arity mismatch on mocks is silent. S485: `app.py` lifespan filters `{m.active_embedding_model for m in metas if m.active_embedding_model}` — the `""` default (CollectionMeta default; `routes_search` does `meta.active_embedding_model or config.embedding_model`) is dropped, so default-model collections are never eager-preloaded; the existing test only used a non-empty `"model-X"`. Falsy-sentinel filters always need the empty/default-branch test.
- **[2026-08-12] (×3) S184 no-match 504**: `routes_search._SEARCH_TIMEOUT_SECONDS=30` wraps the WHOLE pipeline call while `ModelEmbedder.encode`/`ModelReranker.predict` build ONNX on FIRST call inside it — nothing warms the cross-encoder (lifespan preloads embedders only) → first search after boot 504s. Repro: cold-load double on `pipeline._reranker._backend` + monkeypatch the constant down. `results==[]` is NOT assertable — the vector leg always returns neighbours. FIX must be request-time: the repro swaps `_backend` AFTER lifespan, so a startup-only warm-up cannot pass it; `await pipeline.warmup_reranker()` in `routes_search.search` BEFORE the `wait_for` (budget covers the search, not one-off model init) + the same call after `embedder_cache.preload` under `eager_load_embedders`. Do NOT "skip rerank when cold" — ~10 test files use `RerankerBackend` stubs hardcoding `is_warm=False` and assert reranked order.
- **[2026-08-11] (×23) regression-guard files**: `path_home_allowlist.txt` pins (file,lineno,sha) — shifts on any added line above callsite; TWO update sites: the allowlist file AND the `_EXPECTED_CONFIG_LINE_NO` constant in the test — both must move together. OpenAPI snapshot: regen with `--update-openapi-snapshot` on 3.12 (FastAPI version bumps change 422 description string). New SearchConfig field: dataclass+`_apply_toml`+coerce+snapshot tests. `response_model=None` trips `test_no_empty_schemas_remain`. release.sh test gate: guard with `RELEASE_SH_TEST_MODE` AND `DRY_RUN!=1`.
- **[2026-08-14] (×13) CLI HTTP-proxy conversion**: grep `tests/` for all patches of removed symbols first; mock `httpx.get/post/delete` (patch where USED). `ConnectError` before `HTTPError`. Test env key must be 64-char hex. Non-terminal `.get(field, sentinel)` in a `while` loop = silent hang → explicit None + fail-fast. `_helpers._poll_job`'s `_CONNECT_FAIL` branch prints "not running" unconditionally — wrong during warmup (PID alive, port unbound); any fix consulting `_get_service().status().running` MUST also patch `_get_service` in the pre-existing `test_poll_job_connect_error_exits_1`, which today never reaches it: unpatched it hits the REAL `LaunchdSearchService`, so the test's result flips with whether the dev's launchd service happens to be up. Fixed 2026-08-14: `_helpers._server_connect_fail_msg()` (blanket `except Exception` → "not running") called only from `_poll_job`'s `_CONNECT_FAIL` branch — the other ~25 `_SERVER_NOT_RUNNING_MSG` call sites and the `is`-identity guard in `test_cli_260_connection_refused_ux.py` stay untouched.
- **[2026-08-11] (×17) adding a column/kwarg to schema/CollectionMeta**: mirror ALL sites: dataclass, `_row_to_meta`, BOTH meta-write fns, all ctor sites, `_ROUTING_FIELDS` (omit job-id). Still needs `_migrate_<field>()` wired + catalog entry, even if no STORE_SCHEMA_VERSION bump. Threading a new kwarg into an internal call breaks hand-rolled `async def fake_*` monkeypatch stubs in tests — grep `tests/` for `def fake_<method>` and widen their signatures in the same change. Also: new calls added to a module require updating `AsyncMock` return values in test factory helpers in the same diff.
- **[2026-08-09] (×11) job-spawning REST route**: guard→404→create→`transition({QUEUED},RUNNING)` BEFORE `create_task`→`_background_tasks`+`add_done_callback`. 409 via persisted `meta.<job>_job_id` lazy-stale-clear. Declare `response_model=`+`responses=` or snapshot documents empty `{}`. `dry_run` preview must skip the `meta.<job>_job_id`+`update_collection_meta` write. Race: clear `meta.<job>_job_id` BEFORE `job_store.update(DONE/FAILED)`.
- **[2026-07-29] (×6) collection visibility = meta-in-namespace**: gate on `get_collection_meta(name,ns) is not None` (not config only); list unions config+ns_names. Bare `MagicMock` on `get_collection_meta` → stub as `AsyncMock(return_value=None)`. `POST /route` root cause: pass store metas as `initial_metadata` into router.
- **[2026-08-14] (×12) xdist/asyncio/stub**: a repro asserting an INTERNAL timeout fires must never use an external `wait_for` budget shorter than the plausible production value (120 s) — the external one always wins, the bare `TimeoutError('')` is indistinguishable from the hang, and the test is unfixable; instead rebind every numeric `*TIMEOUT*` module constant to 0.1 via `monkeypatch.setattr` and give the external budget 5 s (also forces the fix to expose a constant, cf. `routes_search._SEARCH_TIMEOUT_SECONDS`); 3.12 aliases `asyncio.TimeoutError` to builtin `TimeoutError`, so discriminate on `str(exc)` being non-empty, not on type. To park `EmbedderCache`'s loader, patch `embedder_cache.make_embedder` with a `threading.Event`-blocking sync fn (released in `finally`) — never monkeypatch `asyncio.to_thread`, which mutates stdlib process-wide. a lifespan-only `app.state.<x>` set inside an `if config.<flag>:` branch needs a `= None` default next to `app.state._background_tasks` in `create_app` — otherwise every reader AttributeErrors when the flag is off. Backgrounding lifespan work also removes the "startup returned ⇒ it finished" signal: add a success `logger.info`, since only the failure branch logged before. moving lifespan work into `asyncio.create_task` (eager warm-up: `embedder_cache.preload` + `pipeline.warmup_models`) silently makes every existing test that asserts its effect timing-dependent — with stubs they still pass, but a 2 s stand-in preload proves the race; the task lives on TestClient's portal-thread loop so it CANNOT be awaited from the test's loop → poll `app.state._warmup_task.done()` from the test thread. Keep the pre-flight `logger.warning` inline so caplog tests stay deterministic. to prove the lifespan doesn't block, run `app.router.lifespan_context(app)` enter AND exit inside ONE `create_task` (`async with` + an Event) — wrapping only `__aenter__` in a task makes the mounted FastMCP lifespan raise `ValueError: <Token …> was created in a different Context` on exit, masking the real assertion. module-level stubs must restore immediately; absent-pkg simulation via `monkeypatch.setitem` is vacuous if already imported → subprocess or del+reload. `asyncio.run()` not `get_event_loop()`. `async def` → `AsyncMock`. MCP tests need `xdist_group("mcp")`. Scheduler race (`test_migrate_crash_resume_e2e` "DONE != FAILED"): NOT a flake and NOT fixed by writing FAILED directly — `create_migration()` itself enqueues QUEUED and the 0.1 s tick promotes+completes the job within ~1 tick, so the test just races two writers. Only fix that holds: `with patch.object(JobScheduler, "_tick", lambda self: None)` around the injection. Prove such a race with a probe assert (status still QUEUED after a 1 s sleep), never by re-running.
- **[2026-08-14] (×12) test vacuity**: `assert a in x or b in x` → separate asserts. Guard loops with `assert results` first. Exception-path tests need `assert_awaited_once()`. Override tests start at OPPOSITE value. `MagicMock(name=...)` sets display name, not `.name`. Repro-ing an "URL/arg is malformed" bug with a `return_value` mock is ALWAYS vacuous — the mock answers the bad input the real callee would reject; make `side_effect` faithful (e.g. `urlopen` stand-in raising `URLError("unknown url type: …")` unless the URL starts `http(s)://`). Prove non-vacuity by applying the candidate fix in `git worktree add --detach` + main venv, never by editing production.
- **[2026-08-07] (×2) 3rd-party mass version bump**: reproduce PER-LANE. FastAPI 0.141 wraps included routers in `_IncludedRouter` (no `.path`) → when scanning `app.routes` recurse via `.original_router.routes`. pyarrow 25 SIGSEGVs on macOS/arm64 in mimalloc → `os.environ.setdefault("ARROW_DEFAULT_MEMORY_POOL","system")` darwin-gated in `archon_search/__init__.py`. lancedb 0.36 dropped stable tied-row order → fix test-LOCAL via `monkeypatch` deterministic content-aware embed.
- **[2026-08-03] (×7) LanceDB quirks**: `.limit()` is scan limit not sort-then-limit. `merge_insert(["col1","col2"])` for composite key. `merge_insert().when_matched_update_all()` replaces whole row. Missing table → `ValueError("Table not found")`. `count_rows(filter=)`. `db.close()` sync. SQL via `_where_eq`/`_where_in` never f-strings.
- **[2026-08-12] (×17) install/wizard bugfix gotchas**: wizard base-URL probes must prefix `http://` when the typed answer has no scheme — `ollama.AsyncClient(host="localhost:11434")` normalises (verified) so the value works at runtime and passes config validation (non-empty str only), but `urlopen("localhost:11434/api/tags")` raises `URLError: unknown url type` into the blanket except → false "No models available"; normalise in BOTH the FETCH fn AND `_prompt_ollama_model` (before the `stored == ""` comparison, so `localhost:11434` collapses to the default and leaves the key out of config); the normaliser must split authority from path BEFORE injecting the port (`myhost/path` → `http://myhost:{port}/path`, never `http://myhost/path:{port}`); `[graph].ollama_base_url` IS wizard-written via `WizardFeatures.graph_ollama_base_url` → `config_writer._reconcile_ollama_base_url("graph", …)` — `enrichment/ollama.py` concatenates it into httpx, so normalisation there matters. llama.cpp model list must come from the LOCAL cache (`llama cli -cl`, lines `^\s*\d+\.\s+(.+)$`; `0` case prints only the count) — NOT `{base_url}/v1/models`, which surfaces whatever a running llama-server loaded; cache dir is NOT derivable (LLAMA_CACHE/HF_HOME/XDG_CACHE_HOME/manifests), so shell out; `_fetch_llama_cpp_models()` ends up taking NO args (drop `base_url`, update every call site incl. the repro), and co-edit picker header + "No models available from {url}" text + the `/v1/models` prose in 20_wizard.md/30_configuration.md/60_searching.md/archon-search.toml.example (NOT the OpenAI-shim `/v1/models`, unrelated) + delete the orphaned `_v1_models_response` helper. Repro shape: patch the STDLIB attrs `subprocess.run` + `shutil.which` (post-fix `wizard.py` imports `subprocess` at module level, so `archon_search.install.wizard.subprocess` works too) and assert `urlopen.call_count == 0`; existing tests already patch globally via `archon_search.install.urllib.request.urlopen`. Return bytes unless `text/encoding/universal_newlines` kwarg set so the stub survives either impl. S03 "wizard exits 1" = scripted-stdin OFF-BY-ONE. Second S03: `_ask_yn` docstring said "return default on EOFError OR empty input" but code did `raw in {"y","yes"}` — empty "" always False, ignoring `default=True`; fix: add `if not raw: return default`. Real adjacent defect: `_execute_force_reinstall`'s destructive `input()` had no `EOFError` guard. Docker version: `.git` is dockerignored so hatch-vcs stamps `0.0.0+local` → pass `RELEASE_VERSION` build-arg. S02: detect `ModuleNotFoundError`/`ImportError` in log from a byte-offset snapshotted BEFORE restart, extend deadline ONCE. S194/S201: service plist/unit must propagate ARCHON_SEARCH_DATA_DIR. Positional-stdin e2e alignment oracle: assert `"Invalid value" not in result.output` + `"Invalid choice" not in result.output`. `_set_or_remove` value==default omission cannot express "explicit flag equal to the default" → widen the field to `str|None=None` (S561 `routing_strategy`/`log_format`); 5 mandatory co-edits: `render.py`'s `!= default` bullet guard (else prints "Routing: None"), `_detect_config_hand_edits` must compare against `SearchConfig()` not `WizardFeatures().<field>` (now the None sentinel) — never hardcode the literal, `_ensure_section` stays UNCONDITIONAL (needed before the `elif key in doc[section]` delete-branch lookup — NOT for `config show`, whose `_effective_toml` copies the whole default table when a section is absent), interactive `_ask_choice` still yields a str so `assert features == WizardFeatures()` EOF tests must expect the concrete default, and 20_wizard.md prose "accepted default is never written" must be reworded. Fields NOT using the sentinel still need the `WizardFeatures`↔`SearchConfig` default-parity test. GGUF dir-scan fallback: `HF_HOME` is NOT the HF cache root — upstream `huggingface_hub/constants.py:167-192` resolves hub as `HF_HUB_CACHE`|legacy `HUGGINGFACE_HUB_CACHE`|`$HF_HOME/hub`|`${XDG_CACHE_HOME:-~/.cache}/huggingface/hub`, so scanning only `$HF_HOME/hub` misses both the default (common `huggingface-cli` case) and standard Linux XDG. Verified 3.12: `Path.glob` swallows OSError for missing/unreadable/file roots (per-root `except OSError` = dead code + only uncovered lines) but DOES yield dirs named `*.gguf` → need `is_file()` (also drops dangling symlinks); glob is unordered → `sorted(root.glob(...))` or `setdefault` picks a random label across HF snapshot symlinks. One blanket `try/except Exception` beats a `Path.home()`-only `RuntimeError` guard — `Path(v).expanduser()` raises the SAME error, but wrap EACH env-var root independently so one bad value doesn't discard roots already collected. Strip picker disambiguation qualifiers with `rsplit(" [", 1)[0]` guarded on `endswith("]")` — a greedy `r" \[.*\]$"` truncates filenames that themselves contain `[`. `@functools.cache` on an env-var-dependent fn needs an autouse `cache_clear()` fixture at MODULE/conftest scope, not per-class — a sibling class's call poisons the cache first. Every new `Path.home()` needs a `path_home_allowlist.txt` (file,lineno,sha) entry: consolidate to ONE callsite and re-pin after any edit that shifts it.

## Plan-Making & Agent Process
- **[2026-08-14] (×67) verify before acting**: untracked backlog files ≠ open bugs — use `git ls-files Documentation/Completed/ | grep <ID>` (not bare `ls`) to check committed Completed reports; if found AND tests pass, just trash the stale backlog file and move on. pre-reproduce: `ls Documentation/Completed/ | grep <ID>` + `git log --oneline | grep <ID>` FIRST — both hit = bug *probably* closed, but that pair is NOT proof: run `git log --oneline -- <file the fix touched>` for LATER commits and read the CURRENT source, because a follow-up "fix pre-existing test failures" commit can revert a shipped fix and invert its regression test to assert the buggy behavior, leaving the suite green (248bae1b did exactly this to S561's wizard default-key omission). A green test proves only that code and test agree — diff the test against the DOC contract too. `git log --oneline -- <handler> <test>` + run existing test BEFORE any fix. On /commit, `diff` stray ` copy.md` artifacts vs tracked originals before trashing; exclude net-new reports (e.g. S269) from a backlog→Completed move commit. DA/reviewer "Critical/Major" labels need grep-verification (in a doc review, verify every cited file:line against source — plan claims were ~all accurate but a few were stale). Verify methods/CLI options exist (Read the source). Non-terminal sentinel in `while` loop = silent hang → explicit None + fail-fast. `ls` before trash to confirm which timestamped vs bare-named Completed/ variants exist. A repro test that PASSES may mean a peer agent already fixed it in the working tree (`git diff` first) — prove the test really fails by running it against unmodified HEAD in `git worktree add --detach <tmp> HEAD` with the main venv, then `git worktree remove --force`.
- **[2026-08-14] (×35) subagent lifecycle & handoff**: a subagent spawning its OWN children (nested /iterative-review) may never receive their reports — reviewers ran 40min+ with zero returns, `run_in_background:false` is IGNORED for them, and `TaskStop` is refused ("owned by main session"); after ~2 poke rounds do the review inline yourself. To actually block while waiting, foreground `sleep` is denied but `python3 -c "import time; time.sleep(115)"` works (a bg `sleep` returns instantly and does NOT wait). Unreachable reviewers still ACT: one committed half the reviewed diff despite an explicit "REVIEW ONLY, no commits" instruction — always re-run `git status`/`git log` after a review round and de-duplicate findings they fixed independently. Prove a fix is a real guard by temporarily patching the production line back and re-running the new tests. Explore subagents lack Write tool → return findings inline → orchestrator writes them to files immediately. Session-limit kills late agents — check if partial file was written. Subagents: scoped tests foreground only; coordinator: full suite once at end. Fix-agents silently add unrequested changes (e.g. embed review-ID tags) → diff against literal finding list + final strip pass. bugfix/reviewer subagents repeatedly go IDLE after delegating to their own children without finishing (suite/review/commit) → coordinator must TaskStop the idle agent and drive suite→iterative-review→commit itself; "correctness"-angle DA agents often idle without SendMessage-ing findings → poke via SendMessage or read their output file. **Parallel agents on the same subsystem**: their changes can become hunk-entangled (same lines, merged learnings.md bullet) and unsplittable; deadlock when each waits for the other cost 5+ coordinator round-trips — either serialize in the spawn prompt ("A commits first, then B starts") or accept upfront that a combined commit is the outcome.
- **[2026-08-04] (×9) smoke/subprocess patterns**: use `-o addopts=` not `-p no:xdist`. Session-scoped fixtures use `tmp_path_factory`. `GET /collections` (no slash) → 307. Pair `ARCHON_SEARCH_CONFIG` with DATA_DIR. Seed real text (empty → 0 chunks). `xdist_group("smoke_e2e")` serialises EXECUTION not fixture lifetime.
- **[2026-08-04] (×5) TypeSpec contracts**: `field?: T | null` for optional nullable. `namespace`/`model`/`op`/`enum` reserved — use `ns`. Cross-file refs fail — repeat locally. `tsp compile <f> --no-emit` verifies; `--emit @typespec/openapi3` regenerates yaml. `tsp-output/` may be git-tracked.
- **[2026-08-14] (×23) doc close-out**: the `/ready` contract is asserted in EIGHT places — `Architecture/{110 routes_ready row,110 app.py row,110 _helpers row,120:185,140:65,160}`, `OperatorGuide/{20_monitoring §GET /ready,80_capacity:119}`, `UserManual/{140,160}`, `BREAKING.md` — so a sweep scoped to "the files that describe /ready" always orphans siblings; grep `storage.only`, `_SERVER_NOT_RUNNING_MSG`, `eager_load_embedders`, and `collection_sync` instead. Doc code blocks drift silently from the artifact they quote (`160:151` still showed `--start-period=30s` vs the Dockerfile's 600s) — re-read the source file for every fenced block you touch. `test_docs.py::test_security_guide_home_mention_is_not_data_dir_relocation` is a raw substring scan for `HOME` over SecurityGuide/UserManual/OperatorGuide/DeveloperGuide — writing env-var names like `$HF_HOME`/`$XDG_CACHE_HOME` in any of those docs fails it as a false positive; phrase around it ("the standard huggingface and XDG cache environment variables") rather than widening the gate's `expected` set. NEVER insert a `>` blockquote/paragraph between rows of a Markdown table — it terminates the table and the remaining rows render as a new headerless one (S184's fix agent did this to `140_error_handling_strategy.md`); put the note AFTER the last row. S485 follow-up: `EmbedderCache` needed TWO more fixes — `preload` must EVICT on warm-up failure (cached-but-cold hides repeat failures behind a cache hit) and the ctor must accept `providers`, because `app.py` passed `config.providers` only to `app.state.embedder` while every search embeds through the cache; when fixing a "providers ignored" bug grep ALL `make_embedder(` sites (`sync.py:577,685` still ignore them — separate defect). seeding eager-preload with `config.embedding_model` makes `distinct_models` able to exceed `embedder_cache_size` (default 3) — LRU then evicts a just-warmed model and `list(set(...))` makes the victim nondeterministic; guard with a startup WARNING. S485's doc sweep orphaned `README.md:195` + `archon-search.toml.example:77` (grep the WHOLE tree, incl. non-Documentation/ files). caplog asserts on `archon_search.*` are xdist-safe: autouse `_isolate_archon_logging` (conftest.py:201) forces `propagate=True`. prove the doc's MECHANISM claim, not just the line the bug cited — S485 needed BOTH halves: `app.py` preloading `config.embedding_model` AND `EmbedderCache.preload` awaiting `embedder.embed(["warmup"])`, because `make_embedder` builds a LAZY `ModelEmbedder` (`_model=None` until first `encode()`) so caching alone leaves `is_warm` False and loads zero ONNX weights. Oracle: `await cache.preload([m]); (await cache.get_or_load(m)).is_warm`. Assert the WARM state, never just cache membership. Test-double corollary: a `MagicMock` embedder whose `embed` is not an `AsyncMock` makes `preload` swallow a `TypeError` via `return_exceptions=True` — the test then passes for the wrong reason. ADRs are append-only — ADR-08:82-86 describes the pre-S485 filter; supersede, never edit. update in dependency order (api-reference → component-catalog → CLAUDE.md → user manual). Grep WHOLE docs tree for old invariant strings — per-file scope orphans siblings. Verify example output against production fn. BREAKING.md: no entry for additive-only, but DO add one when a core dep becomes an optional extra. `Documentation/Completed/` archives are off-limits for editing; cleanup (trash) of stale Completed/ files is explicitly sanctioned when their content is superseded by code/tests. For provider additions, grep for ALL provider-list enumerations across docs.
- **[2026-08-05] (×9) plan/tasks iterative-review**: pin resolved IDs for fix-agents. `app.py` lifespan closure line# < body line# but executes AFTER. Privacy trilemma: resolve at policy layer. Text sweeps miss diagram nodes.
